# UX audit fix log

| Timestamp | Summary | Detail |
|---|---|---|
| 2026-08-01 22:30 | Backend fix #1 (security review 2026-08-01): JWT dev secret still usable outside production — the gu… | [chi tiết](docs/fix-log/2026-08-01-backend-fix-1-security-review-2026-08-01.md) |
| 2026-08-01 22:30 | Backend fix #2 (security review 2026-08-01): games involving a guest player were not being saved at … | [chi tiết](docs/fix-log/2026-08-01-backend-fix-2-security-review-2026-08-01.md) |
| 2026-08-01 22:30 | Backend fix #3 (security review 2026-08-01): handleGameEnd persisted a game row to SQLite even when … | [chi tiết](docs/fix-log/2026-08-01-backend-fix-3-security-review-2026-08-01.md) |
| 2026-08-01 22:30 | Backend fix #4 (security review 2026-08-01): a player disconnecting mid-game could be scored a timeo… | [chi tiết](docs/fix-log/2026-08-01-backend-fix-4-security-review-2026-08-01.md) |
| 2026-08-01 22:30 | Backend fix #6 (security review 2026-08-01): kicking a mid-game player during their disconnect grace… | [chi tiết](docs/fix-log/2026-08-01-backend-fix-6-security-review-2026-08-01.md) |
| 2026-08-01 22:30 | Backend fix #7 (security review 2026-08-01): the socket flood-protection middleware in SocketHandler… | [chi tiết](docs/fix-log/2026-08-01-backend-fix-7-security-review-2026-08-01.md) |
| 2026-08-01 22:30 | Backend fix #8 (security review 2026-08-01): the server shipped with zero HTTP security headers — me… | [chi tiết](docs/fix-log/2026-08-01-backend-fix-8-security-review-2026-08-01.md) |
| 2026-08-01 22:30 | Backend fix #9 (security review 2026-08-01): npm test was red on main — DisconnectHandler.test.js's … | [chi tiết](docs/fix-log/2026-08-01-backend-fix-9-security-review-2026-08-01.md) |
| 2026-08-01 22:30 | Backend fix #12-partial (security review 2026-08-01): broadcastLobbyUpdate in state.js:54-56 sent th… | [chi tiết](docs/fix-log/2026-08-01-backend-fix-12-partial-security-review-2026-08.md) |
| 2026-08-01 22:30 | Fix #1: lobby room list is over-dense on desktop and breaks down on mobile — 6-column .room-table wr… | [chi tiết](docs/fix-log/2026-08-01-fix-1-lobby-room-list-is-over-dense.md) |
| 2026-08-01 22:30 | Fix #2: room settings tab mixes three audiences with no visual separation — as host, 9 field-groups … | [chi tiết](docs/fix-log/2026-08-01-fix-2-room-settings-tab-mixes-three-audiences.md) |
| 2026-08-01 22:30 | Fix #5 (M4): dead space above the board before a game starts — the pre-game turn bar is hidden with … | [chi tiết](docs/fix-log/2026-08-01-fix-5-m4-dead-space-above-the-board.md) |
| 2026-08-01 22:30 | Fix #6 (M3): on mobile the whole right panel — including both player slots — sits below a full-viewp… | [chi tiết](docs/fix-log/2026-08-01-fix-6-m3-on-mobile-the-whole-right.md) |
| 2026-08-01 22:30 | Fix #7 (M5): room name disappears from the mobile top bar — .topnav__title is dropped at ≤768px, lea… | [chi tiết](docs/fix-log/2026-08-01-fix-7-m5-room-name-disappears-from-the.md) |
| 2026-08-01 22:30 | Phase 2 infrastructure: Lite/Default/Pro UI-mode system — data-ui-mode attribute on <html>, gvn_ui_m… | [chi tiết](docs/fix-log/2026-08-01-phase-2-infrastructure-lite-default-pro-ui-mode.md) |
| 2026-08-01 22:30 | Surface 1: lobby room cards — Lite identical to Default (phase-1 card+chip already is the Lite treat… | [chi tiết](docs/fix-log/2026-08-01-surface-1-lobby-room-cards-lite-identical-to.md) |
| 2026-08-01 22:30 | Surface 2: create-room modal — Lite gets room name + "Quick match" with everything else behind a clo… | [chi tiết](docs/fix-log/2026-08-01-surface-2-create-room-modal-lite-gets-room.md) |
| 2026-08-01 22:30 | Surface 6: lobby online panel — Lite collapses it to a bare "N online" count with the click-to-expan… | [chi tiết](docs/fix-log/2026-08-01-surface-6-lobby-online-panel-lite-collapses-it.md) |
| 2026-08-01 22:30 | Surface 3: room right panel — Lite hides the score table until a game has actually finished, and hid… | [chi tiết](docs/fix-log/2026-08-01-surface-3-room-right-panel-lite-hides-the.md) |
| 2026-08-01 22:30 | Surface 4: room settings tab, guest branch only — Lite replaces the three read-only label/value rows… | [chi tiết](docs/fix-log/2026-08-01-surface-4-room-settings-tab-guest-branch-only.md) |
| 2026-08-01 22:30 | Surface 5: history replay — Lite hides #btn-analysis and #tree-panel entirely; Default unchanged (bu… | [chi tiết](docs/fix-log/2026-08-01-surface-5-history-replay-lite-hides-btn-analysis.md) |
| 2026-08-01 22:30 | Fix #8: user reported the board canvas color palette is too light and uncomfortable to look at for e… | [chi tiết](docs/fix-log/2026-08-01-fix-8-user-reported-the-board-canvas-color.md) |
| 2026-08-01 22:30 | Fix #9: after Fix #8's theme-aware repaint, the user asked to try a different board palette entirely… | [chi tiết](docs/fix-log/2026-08-01-fix-9-after-fix-8-s-theme-aware.md) |
| 2026-08-01 22:30 | Fix #10: user gave a fully specified replacement "Paper Style" palette after seeing Fix #9's wood th… | [chi tiết](docs/fix-log/2026-08-01-fix-10-user-gave-a-fully-specified-replacement.md) |
| 2026-08-01 22:30 | Fix #11: user asked to lighten the Fix #10 palette further — same light-gray background, but "Light … | [chi tiết](docs/fix-log/2026-08-01-fix-11-user-asked-to-lighten-the-fix.md) |
| 2026-08-01 22:30 | Fix #12: user gave a third light-theme spec — near-white cyan-tinted background #DBFDFF, saturated b… | [chi tiết](docs/fix-log/2026-08-01-fix-12-user-gave-a-third-light-theme.md) |
| 2026-08-01 22:30 | Fix #13: user confirmed Fix #12's blue cross and red circle but said the #DBFDFF background read as … | [chi tiết](docs/fix-log/2026-08-01-fix-13-user-confirmed-fix-12-s-blue.md) |
| 2026-08-01 22:30 | Fix #14: with the board background, cross/circle symbols, and wall tiles locked (Fix #8–#13), user r… | [chi tiết](docs/fix-log/2026-08-01-fix-14-with-the-board-background-cross-circle.md) |
| 2026-08-01 22:30 | Fix #15: small usability request — on hover, highlight the corresponding row/column coordinate label… | [chi tiết](docs/fix-log/2026-08-01-fix-15-small-usability-request-on-hover-highlight.md) |
| 2026-08-01 22:30 | Fix #16: user asked to drop the solid border Fix #14 added to the last-move highlight, keeping fill … | [chi tiết](docs/fix-log/2026-08-01-fix-16-user-asked-to-drop-the-solid.md) |
| 2026-08-01 22:30 | Fix #17: user flagged that the amber/gold highlight from Fix #14 reads too close to the circle's red… | [chi tiết](docs/fix-log/2026-08-01-fix-17-user-flagged-that-the-amber-gold.md) |
| 2026-08-01 22:30 | Fix #18: user reported Fix #17's dark forest-green highlight still read as low-contrast/murky in a s… | [chi tiết](docs/fix-log/2026-08-01-fix-18-user-reported-fix-17-s-dark.md) |
| 2026-08-01 22:30 | Fix #19: user asked to try a vivid, fully-saturated green — #00FF44 — as the highlight, flat across … | [chi tiết](docs/fix-log/2026-08-01-fix-19-user-asked-to-try-a-vivid.md) |
| 2026-08-01 22:30 | Fix #20: user asked to try a saturated yellow — #FFEA00 — as the highlight, flat across both themes. | [chi tiết](docs/fix-log/2026-08-01-fix-20-user-asked-to-try-a-saturated.md) |
| 2026-08-04 14:13 | TODO #45 (1/N): root cause — auth.js error responses now carry a language-neutral `code`; login.js maps `co… | [chi tiết](docs/fix-log/2026-08-04-todo-45-1-root-cause-auth-error-codes.md) |
| 2026-08-04 14:35 | TODO #45 (2/N): socket-level errors (GameEngine, RoomManager, ChatHandler, GameHandler, RoomHandler, LobbyHan… | [chi tiết](docs/fix-log/2026-08-04-todo-45-2-socket-level-error-codes.md) |
| 2026-08-04 14:52 | TODO #45 (3/N): game-ui.js Swap2 opening UI + draw-offer prompt now call t() instead of hardcoded Vietnamese … | [chi tiết](docs/fix-log/2026-08-04-todo-45-3-game-ui-swap2-draw-i18n.md) |
| 2026-08-04 15:14 | TODO #45 (4/N): history.html/history.js wired into the i18n system end-to-end — 46 new keys, data-i18n attrs,… | [chi tiết](docs/fix-log/2026-08-04-todo-45-4-history-page-i18n.md) |
| 2026-08-04 15:32 | TODO #45 (5/5, done): leftover tooltips (room-ui.js, index.html, room.html) + reconnect banner (socket-client… | [chi tiết](docs/fix-log/2026-08-04-todo-45-5-leftover-tooltips-and-dynamic-text.md) |
| 2026-08-04 15:08 | TODO #45 follow-up (user report): 27 more hardcoded Vietnamese server messages found — system chat announceme… | [chi tiết](docs/fix-log/2026-08-04-todo-45-followup-system-chat-and-middleware.md) |
| 2026-08-01 22:48 | Backend TODO Phần B #1 (review 5.1): restart-hang — on connection, SocketHandler.js had if (existing… | [chi tiết](docs/fix-log/2026-08-01-backend-todo-phan-b-1-review-5-1.md) |
| 2026-08-01 22:54 | Backend TODO Phần B #2 (review 3.5): chat sanitization used replace(/<[^>]>/g, '') in server/manager… | [chi tiết](docs/fix-log/2026-08-01-backend-todo-phan-b-2-review-3-5.md) |
| 2026-08-01 23:04 | Backend TODO Phần B #3 (review 3.7): escapeAttr in client/js/lobby.js:474 and client/js/room-ui.js:6… | [chi tiết](docs/fix-log/2026-08-01-backend-todo-phan-b-3-review-3-7.md) |
| 2026-08-02 00:30 | Correction to the fix logged at 2026-08-01 22:48 (TODO Phần B #1, restart-hang). Browser-verifying T… | [chi tiết](docs/fix-log/2026-08-02-correction-to-the-fix-logged-at-2026-08.md) |
| 2026-08-02 01:44 | Backend TODO Phần B #4 (review 6.4): two independent problems on the public game-history API. (a) ge… | [chi tiết](docs/fix-log/2026-08-02-backend-todo-phan-b-4-review-6-4.md) |
| 2026-08-02 01:51 | Backend TODO Phần B #5 (review 5.5): the idle-room cleanup interval in server/managers/RoomManager.j… | [chi tiết](docs/fix-log/2026-08-02-backend-todo-phan-b-5-review-5-5.md) |
| 2026-08-02 02:01 | Backend TODO Phần B #6 (review 3.6): POST /api/auth/login in server/routes/auth.js returned 401 imme… | [chi tiết](docs/fix-log/2026-08-02-backend-todo-phan-b-6-review-3-6.md) |
| 2026-08-02 02:37 | Backend TODO Phần B #7 (review 3.2): RoomManager.createRoom() enforced only a global MAX_ROOMS cap a… | [chi tiết](docs/fix-log/2026-08-02-backend-todo-phan-b-7-review-3-2.md) |
| 2026-08-02 02:45 | Backend TODO Phần B #8 (review 4.2): room:updated re-sent the full room snapshot — users[] + setting… | [chi tiết](docs/fix-log/2026-08-02-backend-todo-phan-b-8-review-4-2.md) |
| 2026-08-02 02:55 | Backend TODO Phần B #9 (review 4.1 + verification report on 3da53dd): broadcastLobbyUpdate re-sent t… | [chi tiết](docs/fix-log/2026-08-02-backend-todo-phan-b-9-review-4-1.md) |
| 2026-08-02 03:35 | Backend TODO Phần B #10 (review 4.3): TimerManager broadcast timer:tick to every member of a room on… | [chi tiết](docs/fix-log/2026-08-02-backend-todo-phan-b-10-review-4-3.md) |
| 2026-08-02 03:44 | Backend TODO Phần B #11 (verification report on 3da53dd): six shipped fixes had no test protecting t… | [chi tiết](docs/fix-log/2026-08-02-backend-todo-phan-b-11-verification-report-on.md) |
| 2026-08-02 03:49 | Backend TODO Phần B #12 (verification report on 3da53dd): in cancelDisconnectGrace (DisconnectHandle… | [chi tiết](docs/fix-log/2026-08-02-backend-todo-phan-b-12-verification-report-on.md) |
| 2026-08-02 03:54 | User report: ./start.sh could not start the server at all — Error: JWT_SECRET must be set (no defaul… | [chi tiết](docs/fix-log/2026-08-02-user-report-start-sh-could-not-start-the.md) |
| 2026-08-02 04:09 | TODO #15 (follow-up carved out of the #13 decision): after Phần B #2 switched chat sanitization to e… | [chi tiết](docs/fix-log/2026-08-02-todo-15-follow-up-carved-out-of-the.md) |
| 2026-08-02 04:17 | TODO #14: socket-client.js bound reconnect_attempt (status banner) and reconnect on this.socket, but… | [chi tiết](docs/fix-log/2026-08-02-todo-14-socket-client-js-bound-reconnect-attempt.md) |
| 2026-08-02 04:29 | TODO #16 + #17 (found while doing Phần B #4, done together per that item's own constraint): getGameB… | [chi tiết](docs/fix-log/2026-08-02-todo-16-17-found-while-doing-phan-b.md) |
| 2026-08-02 17:57 | Capacity stress-testing (docs/stress-test-report.md §9-10, requested by the user to find a real veri… | [chi tiết](docs/fix-log/2026-08-02-capacity-stress-testing-docs-stress-test-report-md.md) |
| 2026-08-02 20:45 | Fix #18 second pass — user reported that on the real deployment (play3cr.dpdns.org) room creation fa… | [chi tiết](docs/fix-log/2026-08-02-fix-18-second-pass-user-reported-that-on.md) |
| 2026-08-02 21:05 | Same test run surfaced a second, distinct crash unrelated to #18: ValidationError: ERR_ERL_UNEXPECTE… | [chi tiết](docs/fix-log/2026-08-02-same-test-run-surfaced-a-second-distinct-crash.md) |
| 2026-08-02 21:20 | TODO.md #30 (flagged in the previous entry, not yet fixed): under Cloudflare Tunnel, socket.handshak… | [chi tiết](docs/fix-log/2026-08-02-todo-md-30-flagged-in-the-previous-entry.md) |
| 2026-08-02 22:41 | TODO.md #29 (unexplained >6000-player ceiling, backlog fix already shipped): re-ran scripts/capacity… | [chi tiết](docs/fix-log/2026-08-02-todo-md-29-unexplained-6000-player-ceiling-backlog.md) |
| 2026-08-02 22:45 | TODO.md #28 (transport order, "measured, deliberately not applied" pending re-evaluation once the ba… | [chi tiết](docs/fix-log/2026-08-02-todo-md-28-transport-order-measured-deliberately.md) |
| 2026-08-03 00:26 | User request: finish the "further" delta step for room:updated that TODO.md #8 deliberately deferred… | [chi tiết](docs/fix-log/2026-08-03-user-request-finish-the-further-delta-step-for.md) |
| 2026-08-03 00:55 | User asked directly whether MAX_ROOMS/MAX_USERS_PER_ROOM could be raised, after being told the 200-t… | [chi tiết](docs/fix-log/2026-08-03-user-asked-directly-whether-max-rooms-max-users.md) |
| 2026-08-03 01:03 | User request: default theme should be light, not dark — the pre-first-paint theme-init IIFE in clien… | [chi tiết](docs/fix-log/2026-08-03-user-request-default-theme-should-be-light-not.md) |
| 2026-08-03 01:14 | Feature request (History tab): allow search/query by player, date, results, plus count-by-date and c… | [chi tiết](docs/fix-log/2026-08-03-feature-request-history-tab-allow-search-query-by.md) |
| 2026-08-03 01:52 | TODO.md Phần B #32 (from the 2026-08-03 whole-codebase security review): isValidDisplayName in serve… | [chi tiết](docs/fix-log/2026-08-03-todo-md-phan-b-32-from-the-2026.md) |
| 2026-08-03 02:33 | TODO.md #35: #start-modal and #game-overlay (win/loss/rematch notification) visually overlapping aft… | [chi tiết](docs/fix-log/2026-08-03-todo-md-35-start-modal-and-game-overlay.md) |
| 2026-08-04 09:00 | TODO.md Phần B #33 (from the 2026-08-03 security-review recheck): acceptDraw()/declineDraw() in serv… | [chi tiết](docs/fix-log/2026-08-04-todo-md-phan-b-33-from-the-2026.md) |
| 2026-08-04 09:20 | TODO.md Phần B #34 (from the 2026-08-03 security-review recheck): game:time_accept/game:time_decline… | [chi tiết](docs/fix-log/2026-08-04-todo-md-phan-b-34-from-the-2026.md) |
| 2026-08-04 09:52 | TODO.md Phần B #37 / instruction.md §B37 (user manual-test report): the timer never ran during Swap2… | [chi tiết](docs/fix-log/2026-08-04-todo-md-phan-b-37-instruction-md-b37.md) |
| 2026-08-04 04:23 | TODO.md #36 / instruction.md §B36 (user-proposed redesign after reviewing the current start-game flo… | [chi tiết](docs/fix-log/2026-08-04-todo-md-36-instruction-md-b36-user-proposed.md) |
| 2026-08-04 04:56 | TODO.md #39 (báo cáo người dùng "Reconnect Logic is not very well"): guest/spectator, hoặc player kh… | [chi tiết](docs/fix-log/2026-08-04-todo-md-39-bao-cao-nguoi-dung-reconnect.md) |
| 2026-08-04 08:00 | TODO.md #44 / instruction.md §44 (review 12.6, xác nhận qua Cloudflare API 2026-08-04): getClientIp(… | [chi tiết](docs/fix-log/2026-08-04-todo-md-44-instruction-md-44-review-12.md) |
| 2026-08-04 05:00 | TODO.md #40 (báo cáo người dùng test thủ công): dán link room.html trần — không ?id= và không phải v… | [chi tiết](docs/fix-log/2026-08-04-todo-md-40-bao-cao-nguoi-dung-test.md) |
| 2026-08-04 08:06 | TODO.md #42 / instruction.md §42 (review 12.5, kiểm chứng 2026-08-02): review nêu cancelEmptyRoomGra… | [chi tiết](docs/fix-log/2026-08-04-todo-md-42-instruction-md-42-review-12.md) |
| 2026-08-04 08:16 | TODO.md #43 / instruction.md §43 (review 12.5, kiểm chứng 2026-08-02): MAX_ROOMS_PER_IP đếm quota bằ… | [chi tiết](docs/fix-log/2026-08-04-todo-md-43-instruction-md-43-review-12.md) |
| 2026-08-04 09:00 | TODO.md #41 / instruction.md §41 (review 12.5, kiểm chứng 2026-08-02): ONLINE_USERS_DEBOUNCE_MS 300ms gần vô dụ… | [chi tiết](docs/fix-log/2026-08-04-todo-md-41-debounce-lobby-online-users.md) |
| 2026-08-04 09:18 | TODO.md #29 (follow-up to TODO #41 debounce fix): re-ran scripts/capacity-test at 6000/8000 players, … | [chi tiết](docs/fix-log/2026-08-04-todo-md-29-unexplained-8000-player-ceiling.md) |
| 2026-08-04 09:45 | User report "time plus not work": timer-increment ("Cộng thêm") field only applies server-side in Bl… | [chi tiết](docs/fix-log/2026-08-04-time-plus-not-working-outside-blitz-mode.md) |
| 2026-08-04 13:45 | Fix #21: multi-step user request to rework the stone-mode board — WCAG-solved darker/medium-contrast… | [chi tiết](docs/fix-log/2026-08-04-fix-21-stone-mode-board-wood-redesign.md) |
| 2026-08-04 16:10 | TODO.md #47 (phát hiện phụ khi làm follow-up #45): thông báo mất-kết-nối/kết-nối-lại hiện trùng lặp … | [chi tiết](docs/fix-log/2026-08-04-todo-47-disconnect-reconnect-chat-duplicate.md) |
| 2026-08-06 00:59 | TODO.md #22 (user-selected follow-up to a broadcast-performance audit): `broadcastRoomUpdate` fired s… | [chi tiết](docs/fix-log/2026-08-06-todo-22-room-updated-join-burst-debounce.md) |
| 2026-08-06 03:17 | Code review follow-up on the `pairing_changed` batching fix: `broadcastTournamentDetail` still sent … | [chi tiết](docs/fix-log/2026-08-06-tournament-updated-entries-diff.md) |
| 2026-08-06 15:42 | TODO.md #46: login.js password-toggle aria-label always showed the raw i18n key (`login.hide_pas… | [chi tiết](docs/fix-log/2026-08-06-todo-46-login-js-password-toggle-i18n-fallback.md) |
| 2026-08-06 16:42 | TODO.md #49: tournament match board renders too small/inconsistent vs. room.html — CSS max-width + b… | [chi tiết](docs/fix-log/2026-08-06-todo-49-tournament-match-board-too-small.md) |
| 2026-08-06 21:45 | User report (kèm log, chỉ mở 1 tab): "đăng nhập thiết bị khác" hiện ra và bị đăng xuất nhầm — single-… | [chi tiết](docs/fix-log/2026-08-06-session-kicked-false-positive-on-reconnect.md) |
| 2026-08-06 22:46 | Follow-up: false-kick persisted after the above fix — root cause was `tournaments.js` importing `lo… | [chi tiết](docs/fix-log/2026-08-06-tournaments-lobby-duplicate-module-import.md) |
| 2026-08-07 06:29 | TODO.md #54: "Quay lại danh sách giải đấu" back link from tournament detail page always landed on … | [chi tiết](docs/fix-log/2026-08-07-todo-54-tournament-back-link-tab.md) |
| 2026-08-07 06:40 | TODO.md #53: create-tournament modal had no UI for series mode (race-to-margin/fixed count) though b… | [chi tiết](docs/fix-log/2026-08-07-todo-53-tournament-create-series-mode-ui.md) |
| 2026-08-07 06:59 | TODO.md #52: tournament-match.html UX kém — `.lobby-layout` phantom 260px sidebar column + `.match-s… | [chi tiết](docs/fix-log/2026-08-07-todo-52-tournament-match-layout-dead-space.md) |
| 2026-08-07 07:28 | Follow-up on #52 (user: "Make as Room"): full refactor to reuse room.html's DOM/CSS directly, bundl… | [chi tiết](docs/fix-log/2026-08-07-todo-52-full-room-refactor.md) |
| 2026-08-07 08:02 | TODO.md #58: tournament-match.html never read the saved Paper/Stone board-display setting, always r… | [chi tiết](docs/fix-log/2026-08-07-todo-58-tournament-match-stone-board-style.md) |
| 2026-08-07 10:13 | User report (screenshot from UI/UX review): series-transition overlay showed the same ⏭️ icon for w… | [chi tiết](docs/fix-log/2026-08-07-tournament-match-result-icon-consistency.md) |
| 2026-08-07 10:18 | TODO.md #56 item 2: tournament.html reused .lobby-layout's 1fr 260px grid without ever rendering a .… | [chi tiết](docs/fix-log/2026-08-07-todo-56-tournament-detail-lobby-layout-phantom-sidebar.md) |
| 2026-08-07 10:28 | User report: "Đang chuẩn bị ván tiếp theo..." got stuck, server not moving to game 2 of a series. R… | [chi tiết](docs/fix-log/2026-08-07-series-transition-overlay-dead-end.md) |
| 2026-08-07 10:58 | TODO.md #57: tournament matches only had Đầu hàng (Resign) — added Cầu hoà (Draw) and Xin thêm giờ … | [chi tiết](docs/fix-log/2026-08-07-todo-57-tournament-match-draw-time-request.md) |
| 2026-08-07 23:48 | TODO.md #61: `.match-clocks` quá sát `#match-meta` trên `tournament-match.html` (PC) — thêm `paddin… | [chi tiết](docs/fix-log/2026-08-07-todo-61-tournament-match-clocks-meta-spacing.md) |
| 2026-08-08 08:27 | TODO.md #62: series check-in "Sẵn sàng" giữa các ván bắt quay lại trang giải đấu — chuyển check-in v… | [chi tiết](docs/fix-log/2026-08-08-todo-62-series-checkin-in-place.md) |
| 2026-08-08 08:48 | TODO.md #65: CSP tắt hoàn toàn (`contentSecurityPolicy: false`) + script không pin từ unpkg.com đọc … | [chi tiết](docs/fix-log/2026-08-08-todo-65-csp-third-party-script.md) |
| 2026-08-08 09:10 | Follow-up TODO.md #65: recheck độc lập phát hiện `dist/` production build không được rebuild (còn u… | [chi tiết](docs/fix-log/2026-08-08-todo-65-csp-production-build-gaps.md) |
| 2026-08-08 09:43 | TODO.md #66: `POST /api/auth/{login,register,guest}` trả JWT nhưng không set `Cache-Control` — thêm … | [chi tiết](docs/fix-log/2026-08-08-todo-66-auth-cache-control-no-store.md) |
| 2026-08-08 10:00 | TODO.md #69: self-hosted Manrope fonts (dropped fonts.googleapis.com/gstatic.com + dead Inter link)… | [chi tiết](docs/fix-log/2026-08-08-self-host-google-fonts-and-audio.md) |
| 2026-08-08 12:55 | TODO.md #68: JWT trong `localStorage` → **phiên mờ phía server** + `HttpOnly` cookie (theo chuẩn OW… | [chi tiết](docs/fix-log/2026-08-08-todo-68-server-side-sessions-httponly-cookie.md) |
| 2026-08-08 13:36 | TODO.md #70: style nút bấm không nhất quán toàn `client/` — gộp `--c-danger`→`--c-error`, xoá xanh l… | [chi tiết](docs/fix-log/2026-08-08-todo-70-button-style-inconsistency.md) |
| 2026-08-08 14:56 | TODO.md #72: User report "Display (Paper/Stone) auto get back" — CSP `scriptSrcAttr:'none'` (TODO.… | [chi tiết](docs/fix-log/2026-08-08-todo-72-room-settings-csp-onchange-blocked.md) |
| 2026-08-08 15:12 | Fix #21 follow-up: stone-mode wall/brick colors — WCAG-solved warm-stone palette (mortar/dark/base/… | [chi tiết](docs/fix-log/2026-08-08-stone-mode-wall-brick-colors.md) |
| 2026-08-08 15:25 | TODO.md #73: `.btn`/`.btn-confirm` had no unscoped base rule outside `.modal__actions` — tournamen… | [chi tiết](docs/fix-log/2026-08-08-todo-73-btn-confirm-missing-base-style.md) |
| 2026-08-08 15:41 | TODO.md #74: tournament matches had no sound (missing `audio-manager.js` + no calls at all) and no… | [chi tiết](docs/fix-log/2026-08-08-todo-74-tournament-match-sound-display-mode.md) |
| 2026-08-08 16:16 | TODO.md #64 follow-up: `buildPairingLookup()` let a voided pairing shadow its replay in the Cross T… | [chi tiết](docs/fix-log/2026-08-08-cross-table-replay-pairing-lookup.md) |
| 2026-08-08 18:31 | TODO.md #75: Round Robin Cross Table now sorts rows/columns by current rank (`computeStandings()` … | [chi tiết](docs/fix-log/2026-08-08-todo-75-cross-table-rank-sort-highlight.md) |
| 2026-08-08 19:04 | TODO.md #76: match clock starts server-side the instant both players check in ready, but client req… | [chi tiết](docs/fix-log/2026-08-08-todo-76-tournament-ready-auto-enter.md) |
| 2026-08-09 09:23 | TODO.md #79: `renderHeader()` gán panel `clock-black-name`/`clock-white-name` theo vị trí mảng `players[0]`/`[1]` cố định thay vì theo `.color` thực tế — sai trong Swap2 (P2 chọn "black") và series nhiều ván (seat xoay mỗi ván, TimerManager slot cố định theo entryId)… | [chi tiết](docs/fix-log/2026-08-09-todo-79-tournament-match-timer-wrong-side.md) |
| 2026-08-09 09:33 | TODO.md #80: đồng hồ trận đấu giải đấu thiếu animation `timer-pulse` khi còn ≤10s — màu đỏ + beep đã có từ #74 nhưng không nhấp nháy như phòng thường, khiến cảnh báo dễ bị bỏ lỡ… | [chi tiết](docs/fix-log/2026-08-09-todo-80-tournament-clock-low-time-pulse.md) |
| 2026-08-09 10:04 | TODO.md #82: đăng ký/hủy đăng ký giải đấu bắn thêm 1 round-trip `tournament:get` thừa sau khi server đã tự broadcast `tournament:updated` đầy đủ dữ liệu — xoá 2 listener phụ ở `tournament-detail.js`… | [chi tiết](docs/fix-log/2026-08-09-todo-82-tournament-register-redundant-get.md) |
| 2026-08-09 10:11 | TODO.md #83: `tournament:register`/`unregister` bắn `broadcastTournamentDetail` ngay lập tức, không debounce như `_queuePairingChanged` — thêm `_queueTournamentDetailUpdate` gộp burst cùng tournamentId trong 1 tick qua `setImmediate`… | [chi tiết](docs/fix-log/2026-08-09-todo-83-tournament-register-broadcast-debounce.md) |
| 2026-08-09 10:31 | TODO.md #84: tab "Lịch sử ván đấu" giải đấu không phân trang — `getTournamentGames()` trả hết, client dựng cả bảng bằng `innerHTML` đồng bộ — thêm `page`/`limit` theo khuôn mẫu `routes/games.js`, UI phân trang `‹ 1 2 ›`… | [chi tiết](docs/fix-log/2026-08-09-todo-84-tournament-games-history-pagination.md) |
| 2026-08-09 12:11 | TODO.md #87: `broadcastLiveMatchesUpdate` không debounce/diff — huỷ giải đấu có N ván live bắn N emit liên tiếp qua vòng lặp `forceCancelMatch` — coalesce qua `setImmediate` (khuôn mẫu `_queuePairingChanged`) + skip nếu list không đổi… | [chi tiết](docs/fix-log/2026-08-09-todo-87-live-matches-broadcast-throttle-diff.md) |
| 2026-08-09 12:38 | TODO.md #88: khán giả/guest xem trận giải đấu bị khoá nút "Quay lại chi tiết giải đấu" y hệt 2 người chơi thật — `setLeaveLocked(true)` (3 nơi gọi) không kiểm tra `myPlayer()` — gate cả 3 nơi theo `myPlayer()`, bỏ hẳn lock mù lúc load trang… | [chi tiết](docs/fix-log/2026-08-09-todo-88-tournament-match-spectator-leave-lock.md) |
| 2026-08-09 12:45 | TODO.md #88 follow-up: thêm unit test thật cho fix (`client/tests/tournament-match-leave-lock.test.js`, 7 case, jsdom + fixture HTML thật) — cài `jest-environment-jsdom`, đây là unit test client-side đầu tiên của repo — xác nhận qua mutation test (bản copy tạm, không commit) đúng 4/7 case fail khi revert fix… | [chi tiết](docs/fix-log/2026-08-09-todo-88-followup-unit-test.md) |
| 2026-08-09 13:06 | TODO.md #89: `npm audit` báo 3 lỗ hổng high-severity transitive (`socket.io-parser`, `js-yaml`, `nanoid`) — `npm audit fix` (không `--force`) resolve cả 3 semver-safe cùng lúc, chỉ đổi `package-lock.json`, không cần bump `socket.io`/`jest`/`vite`… | [chi tiết](docs/fix-log/2026-08-09-todo-89-npm-audit-transitive-deps.md) |
| 2026-08-09 15:21 | TODO.md #90: trang trận đấu giải đấu thỉnh thoảng tự scroll khi click bàn cờ — `updateBoardState()` gọi `boardRenderer.resize()` (đổi kích thước canvas, ảnh hưởng layout) sau MỌI nước đi, khác Tables Room; bỏ lời gọi đó, chỉ giữ resize ở init/window-resize… | [chi tiết](docs/fix-log/2026-08-09-todo-90-tournament-match-resize-scroll-jump.md) |
| 2026-08-09 18:19 | TODO.md #92: `authLimiter` dùng `req.ip` mặc định của `express-rate-limit` — đằng sau Cloudflare Tunnel mọi client thật gộp thành 1 "IP" quan sát được, chia sẻ chung 1 ngân sách 20 request/15 phút; thêm `get-client-ip.js` tái dùng CF-Connecting-IP logic từ #44, gắn `keyGenerator`… | [chi tiết](docs/fix-log/2026-08-09-auth-rate-limit-shared-ip-behind-tunnel.md) |
| 2026-08-09 23:38 | User report bad word filter too strong — `filterMessage()` giờ chỉ exact-match sau normalize; tắt fuzzy edit-distance ("distance scale") và classifier reject-stage ("SVM") trong pipeline, giữ nguyên normalize (repeat-collapse) + dictionary. Fix trên `main` qua PR #3… | [chi tiết](docs/fix-log/2026-08-09-profanity-filter-disable-fuzzy-classifier.md) |
| 2026-08-09 23:54 | Đồng bộ fix filter chửi thề (trên) sang `dev` — `dev` đã phân kỳ khỏi `main` trên mọi file `?v=` nên áp trực tiếp cùng thay đổi vào `dev` thay vì merge `main`, bump `?v=96`→`?v=97` theo số của `dev`… | [chi tiết](docs/fix-log/2026-08-09-profanity-filter-disable-fuzzy-classifier-dev-sync.md) |
| 2026-08-10 11:31 | TODO.md #95: OAuth state cookie tên cố định (`gvn_oauth_state`) bị đè khi 2 lần thử Google gần-đồng-thời — đổi tên cookie sang self-describing theo giá trị state (`gvn_oauth_state_<state>`), callback kiểm tồn tại cookie thay vì so 2 giá trị, validate format state trước khi dựng tên cookie… | [chi tiết](docs/fix-log/2026-08-10-todo-95-oauth-state-cookie-collision.md) |
| 2026-08-10 11:52 | TODO.md #96: `GET /google/callback` không idempotent khi request bị lặp lại — thêm kiểm tra session hợp lệ hiện có khi state cookie đã bị dùng, redirect `/index.html` thay vì báo lỗi giả nếu đã có phiên hợp lệ từ lần xử lý trước… | [chi tiết](docs/fix-log/2026-08-10-todo-96-oauth-callback-idempotent.md) |
| 2026-08-10 12:15 | TODO.md #101: cookie state OAuth tự viết tay flags thay vì tái dùng `baseCookieOptions()` — thêm tham số `path` cho `baseCookieOptions()`, đổi cả set/clear ở `auth.js` gọi chung hàm đó; phát hiện thêm `baseCookieOptions` chưa từng được export, sửa luôn… | [chi tiết](docs/fix-log/2026-08-10-todo-101-oauth-state-cookie-reuse-helper.md) |
| 2026-08-10 12:35 | TODO.md #97: tên hiển thị Google có dấu câu (`O'Brien`, `Marks & Co`) bị `isValidDisplayName()` từ chối rồi âm thầm thay tên khách ngẫu nhiên — thêm `sanitizeOAuthDisplayName()` riêng cho nhánh OAuth, strip ký tự bị chặn thay vì reject toàn bộ, giữ `isValidDisplayName()` gốc không đổi cho form đăng ký… | [chi tiết](docs/fix-log/2026-08-10-todo-97-oauth-display-name-sanitize.md) |
| 2026-08-10 12:55 | TODO.md #98: `GET /google` (JSON) và `/google/callback` (text) xử lý lỗi "chưa cấu hình" khác nhau, cả 2 thoát khỏi UI lỗi login.html có style — cả 2 route giờ redirect `login.html?error=oauth_not_configured`, thêm i18n key riêng, bump `?v=98`→`?v=99`… | [chi tiết](docs/fix-log/2026-08-10-todo-98-oauth-not-configured-ui.md) |
| 2026-08-10 13:20 | TODO.md #99: `login.js`'s `checkExistingSession()` redirect chạy trước banner lỗi OAuth — skip redirect khi URL có `error=`, thêm test jsdom đầu tiên cho `login.js`, bump `?v=99`→`?v=100`… | [chi tiết](docs/fix-log/2026-08-10-todo-99-login-oauth-error-banner.md) |
| 2026-08-10 13:40 | TODO.md #100: migration `idx_users_oauth` (#94) chạy lại dò-trùng + rebuild mỗi lần boot dù đã unique — thêm guard `PRAGMA index_list('users')`, khớp khuôn mẫu 4 migration khác trong file… | [chi tiết](docs/fix-log/2026-08-10-todo-100-oauth-index-migration-guard.md) |
| 2026-08-10 11:54 | TODO.md #102 (phần 1+2): `login.js`/`oauth-complete.js` trùng logic lưu-user-rồi-redirect — gộp vào `GvnSession.completeLogin()`; bỏ SELECT thừa sau INSERT ở nhánh tạo user Google mới. Phần 3 (bỏ `oauth-complete.html`) cố ý không làm theo đúng hướng dẫn… | [chi tiết](docs/fix-log/2026-08-10-todo-102-oauth-client-duplication.md) |
| 2026-08-11 22:29 | TODO.md #104: mobile chạm bảng cờ làm chatbox active & trang tự cuộn — dời `e.preventDefault()` lên trước guard sớm-thoát trong `board.js`'s `_onTouchEnd`, thêm `touch-action: none` cho `.board-canvas-wrap canvas` khớp `#match-canvas`; unit test jsdom mới xác nhận bắt được lỗi trên code cũ, bump `?v=101`→`?v=102`… | [chi tiết](docs/fix-log/2026-08-11-todo-104-mobile-touch-scroll-chatbox.md) |
| 2026-08-12 00:00 | User report: focus mode's bottom-right icon (CSS overlay, not real Fullscreen) leaves a big empty gap below the board — `.room--focus`'s `padding-bottom: 140px` + `board.js`'s matching `bottomReserve` reserved far more space than `#chat-input-wrapper`/`.btn-focus` actually need; moved chat input to `bottom: 20px` to hug `.btn-focus`, shrank both reserves 140→80px… | [chi tiết](docs/fix-log/2026-08-12-focus-mode-bottom-gap.md) |
| 2026-08-12 00:07 | TODO.md #103 follow-up: merge #103 (WALL rule 2nd-move Chebyshev distance) thêm mã lỗi server `WALL_SECOND_MOVE_MIN_DISTANCE` nhưng quên thêm key i18n tương ứng — client hiện chuỗi khoá thô thay vì thông báo thật; thêm `err.wall_second_move_min_distance` (vi+en), unit test jsdom xác nhận bắt lỗi hồi quy, bump `?v=102`→`?v=103`… | [chi tiết](docs/fix-log/2026-08-12-todo-103-followup-wall-second-move-i18n-key.md) |
| 2026-08-12 05:35 | TODO.md #107: cả 4 trang nạp `/socket.io/socket.io.js` bản debug 155 836 B — đổi sang `socket.io.min.js` 46 822 B (−109 KB/trang, −70%); cố ý KHÔNG thêm `?v=` vào URL này và KHÔNG bump `?v=N` (chỉ đụng `client/*.html`); xác minh bằng trình duyệt thật qua domain (socket kết nối, online list có dữ liệu, không lỗi console)… | [chi tiết](docs/fix-log/2026-08-12-todo-107-socket-io-min-js.md) |
| 2026-08-12 05:52 | TODO.md #106: `express.static` không option → `Cache-Control: public, max-age=0` ép revalidate mọi asset; thêm `server/config/staticCache.js` (`*.html` → `no-cache`, còn lại → `max-age=31536000, immutable`) + đặt header tường minh cho nhánh SPA catch-all; 17 unit test mới hai tầng (hàm chính sách + mount `express.static` thật), case 304 dùng `http.get` thô vì undici cache biến 304 thành 200; **còn thiếu bước đo qua CF, chờ người dùng khởi động lại server**… | [chi tiết](docs/fix-log/2026-08-12-todo-106-static-cache-control.md) |
| 2026-08-12 06:12 | TODO.md #108 giai đoạn 1 (a)+(c): bỏ `phosphor/bold/style.css` khỏi `index.html`+`login.html` (−235 813 B/trang) và đổi `font-display: block` → `swap` ở cả 2 weight; **giữ link bold ở `tournament.html`** trái với bảng trong file TODO vì `tournament-detail.js:342` sinh `ph-bold` từ JS; (b) subset cố ý không làm (tên icon ghép động ở `tournament-match.js`), bump `?v=103`→`?v=104`, xác minh từng icon trên 6 trang × 2 viewport… | [chi tiết](docs/fix-log/2026-08-12-todo-108-phosphor-bold-font-display.md) |
| 2026-08-12 06:41 | TODO.md #106 xác minh nốt (người dùng dừng server, tự chạy bản riêng với DB thật dời sang bên rồi khôi phục, md5 khớp): origin `immutable`/`no-cache`/`no-store` đúng cả 3 loại, Cloudflare `REVALIDATED` → **`HIT`**, trình duyệt thật lần vào lại từ 25 request revalidate xuống **0 request / 0 KB**; `index.html` vẫn `DYNAMIC` đúng thiết kế; **không** kết luận đã xoá triệu chứng "sometime lag" vì cả 2 lần đo TTFB đều không tái hiện đỉnh ~1s của baseline… | [chi tiết](docs/fix-log/2026-08-12-todo-106-verification-through-cloudflare.md) |
| 2026-08-12 07:53 | Đo lại sau #106/#107/#108 (server thật đang tắt → chạy bản tạm cổng 3001, DB thật dời sang bên rồi khôi phục, md5 khớp; crawl đệ quy cả đồ thị request thay vì kiểm file mẫu): trang sảnh 25 request/570 164 B, **23/25 đã `immutable`**; phát hiện `/socket.io/socket.io.min.js` vẫn `max-age=0` vì do `serveClient` phục vụ, ngoài tầm `express.static` mà #106 sửa → mở **#111**; kết luận #105 vẫn đáng làm (327 KB text chưa nén, gzip −76%), #109 hạ ưu tiên & đổi phạm vi (`dist/` nay còn thiếu cả #107+#108, bật production sẽ xoá fix vừa làm), #110 đề nghị đóng (lợi thật chỉ 4 795 B brotli)… | [chi tiết](docs/fix-log/2026-08-12-do-lai-sau-106-107-108-quyet-dinh-105-109-110.md) |
| 2026-08-12 08:12 | TODO.md #105: thêm `compression` + `app.use(compression())` trước `express.static` (giữ mặc định level 6 và bộ lọc `compressible`, KHÔNG đụng `perMessageDeflate`); 14 unit test mới theo khuôn #106 (mount thật + `http` thô vì undici tự giải nén làm assert vô nghĩa), trong đó 3 test "wiring" đọc source `index.js` — bản đầu vô nghĩa vì regex khớp trúng dòng comment, đã lọc comment rồi **kiểm chứng bằng cách comment tạm dòng thật để thấy test đỏ**; đo server thật: trang sảnh 570 164 → **290 728 B trên dây (−49,0%)**, woff2 không bị nén lại, #66/#106 nguyên vẹn, `npm test` 1101/1101… | [chi tiết](docs/fix-log/2026-08-12-todo-105-compression-middleware.md) |
| 2026-08-12 08:44 | TODO.md #111: **cả 2 hướng sửa trong instruction đều bị probe bác bỏ** — engine.io chiếm trọn prefix `/socket.io/` ở tầng HTTP server (Express không hề thấy request; `serveClient:false` chỉ khiến nó trả 400 chứ không rơi xuống `express.static`) và `max-age=0` là hardcode ở `socket.io/dist/index.js:360`; nên phục vụ file từ `/vendor/socket.io/` (`max-age=86400`, **không** `immutable` vì URL không có `?v=N`) qua export riêng `socketIoClientOptions`, giữ `serveClient` BẬT để HTML cũ/`dist/` cũ không vỡ, đổi `src` ở 4 file HTML; Chromium thật: WS mở, online-count=1, **lần vào lại 25/25 resource từ cache, 0 byte qua mạng**; 13 test mới, `npm test` 1114/1114… | [chi tiết](docs/fix-log/2026-08-12-todo-111-socket-io-client-cache-control.md) |
| 2026-08-12 09:05 | TODO.md #109 (người dùng chọn phương án sau khi được hỏi, đúng điều kiện tiên quyết của instruction) + #110 đóng: bỏ hẳn nhánh `NODE_ENV==='production' ? dist : client` trong `server/index.js`, xoá thư mục `dist/` cục bộ (3 MB/43 file, **không** được git theo dõi nên chỉ là dọn artifact), cập nhật `README.md`, 4 test mới chặn ai đó thêm lại nhánh env-conditional; **deviation có chủ ý** so với instruction ("không xoá `dist/`") vì #105+#106+#111 đã kéo lần-vào-lại xuống 0 byte nên bundle hết tác dụng, và bỏ nhánh thì không còn bản sao thứ hai để lệch pha; xác minh bằng cách chạy thật **với `NODE_ENV=production`** → phục vụ đúng `client/` hiện tại (có `/vendor/socket.io/` của #111, thứ `dist/` cũ không có); #110 đóng vì lợi thật chỉ 4 795 B brotli; `npm test` 1118/1118… | [chi tiết](docs/fix-log/2026-08-12-todo-109-remove-stale-dist-branch.md) |
| 2026-08-12 09:31 | Xác minh nhóm #105-#111 qua domain thật sau khi người dùng khởi động lại server (chỉ đọc, không đụng tiến trình của họ): qua Cloudflare mọi asset `br` + `immutable` + **HIT**, `/vendor/socket.io/` (#111) `max-age=86400` MISS→HIT, URL socket.io cũ vẫn 200 (lưới an toàn còn nguyên), `/api/auth/me` vẫn `no-store`; Chromium thật: lần đầu 27 resource/**203 928 B**, **lần vào lại 0 request / 0 byte**, WS mở, online=1; TTFB `index.html` ×10 đều 0.285-0.306s **không đỉnh nào** (baseline 2/10 vọt 0.88-1.03s) — nhưng **chưa kết luận đã hết "sometime lag"**, chỉ chắc được số round-trip về origin mỗi lần tải trang đã từ 26 xuống 1; phát hiện phụ → mở **#112** (beacon Cloudflare Insights bị CSP chặn, đã kiểm chứng không do repo: HTML origin vs CF giống hệt từng byte)… | [chi tiết](docs/fix-log/2026-08-12-xac-minh-nhom-105-111-qua-domain-that.md) |
| 2026-08-12 10:12 | TODO.md #112 (người dùng chọn hướng B: giữ analytics, nới CSP): **phát hiện then chốt khi ĐO thay vì đoán — beacon tải từ `static.cloudflareinsights.com` nhưng POST số liệu tới `cloudflareinsights.com/cdn-cgi/rum`, HAI host khác nhau** (đọc thẳng URL tuyệt đối trong `beacon.min.js`); chỉ allowlist host script (điều mà đoán sẽ làm) thì beacon chạy nhưng vẫn không gửi được gì — vẫn "không có dữ liệu", chỉ đổi loại lỗi console; thêm đúng 2 host pin tuyệt đối (không wildcard, không `unsafe-*`) vào `scriptSrc`+`connectSrc`; Chromium thật **kèm nhóm đối chứng**: beacon LOADED + POST không bị CSP chặn, còn `unpkg.com`/`example.com` vẫn BLOCKED (chứng minh không nới quá tay); 6 test mới, `npm test` 1124/1124; **chưa xác minh end-to-end được vì domain thật đang chạy tiến trình có CSP cũ — cần restart**… | [chi tiết](docs/fix-log/2026-08-12-todo-112-csp-cloudflare-insights.md) |
| 2026-08-12 10:47 | Xác minh #112 end-to-end trên domain thật sau khi người dùng restart (chỉ đọc, **không dời DB** vì server thật đang chạy): **3 lỗi CSP/lần tải → 0**, `beacon.min.js` HTTP 200, `window.__cfBeacon` = object; #105-#111 không hồi quy (WS kết nối, online=1, lần đầu 27 resource/204 266 B). Quan sát làm rõ: beacon thực tế POST về **`/cdn-cgi/rum` trên chính domain mình** (300 B) — đúng "trường hợp thứ 3" đã dự đoán từ `window.location.origin`, Cloudflare xử lý ở biên nên **không tới server** (origin trả 404); do đó `connect-src https://cloudflareinsights.com` hiện KHÔNG được dùng tới, cố ý giữ làm dự phòng. **Đính chính** entry 09:31: lần vào lại nay là 1 request/300 B (POST analytics) chứ không còn 0 byte — là cái giá của việc bật Web Analytics, không phải hồi quy #106/#111… | [chi tiết](docs/fix-log/2026-08-12-xac-minh-112-end-to-end-sau-restart.md) |
| 2026-08-12 11:15 | TODO.md #108 đóng hẳn (người dùng chốt): GĐ1 (a)+(c) đã làm từ trước, GĐ2 (subset 45 icon) chuyển từ "khuyến nghị đóng, chờ quyết định" sang **quyết định cuối: không làm** — lý do không đổi, `tournament-match.js:721,760` ghép tên class icon động nên con số 45 chỉ là cận dưới đo được bằng grep tĩnh, subset trên cơ sở đó có nguy cơ icon biến mất im lặng; cập nhật `docs/todo/B108-*.md` + `TODO.md` (⏳→✅) + `instruction.md`, không có thay đổi code, không cần nhánh riêng (thuần tài liệu). Nhóm #105-#112 nay hoàn tất toàn bộ, không còn mục ⏳… | [chi tiết](docs/fix-log/2026-08-12-todo-108-dong-han-giai-doan-2-subset-icon.md) |
| 2026-08-13 15:39 | `ui/zen-minimal`: chuỗi báo cáo board/chat của Zen Panel Drawer — box-shadow + `:focus-visible` khiến chatbox còn viền; board méo 825×875 do 1 border bọc cả canvas lẫn turn-bar/game-controls (dời border sang `.board-canvas-wrap`, phần tử luôn vuông); `board.js` còn trừ overhead của thiết kế Double-Bezel cũ dù zen đã phẳng; `.board-area` chuyển `height:100%` của shell để hết khoảng hụt do ước lượng; gap 52px cuối cùng do `.game-controls` giữ chỗ dù rỗng trước ván — `:empty` collapse + `resize()` bắn lại khi controls render thật; có 1 lần chẩn đoán sai (tưởng là DevTools overlay) được người dùng sửa lại bằng chính số đo… | [chi tiết](docs/fix-log/2026-08-13-zen-room-board-sizing-and-chat-input.md) |
| 2026-08-14 12:00 | TODO.md #115: Viewer (`slot === null`) mất kết nối bị `startSpectatorGrace()` đuổi sau 30s giống seated-player-chưa-`ongoing` — tách nhánh trong `handleDisconnect()` theo `slot`: Viewer chỉ set `presence = 'disconnected'` + broadcast, không set timeout nào, ở lại `room.users` vô thời hạn (miễn phòng còn tồn tại); seated player chưa vào ván giữ nguyên 30s cũ; 3 test mới + cập nhật đếm call-site inventory (B113) 21→22, `npm test` 1134/1134… | [chi tiết](docs/fix-log/2026-08-14-todo-115-viewer-reconnect-unlimited.md) |
| 2026-08-14 13:00 | TODO.md #116: `.score-panel` (bảng điểm) trên `room.html` nằm cố định trong `.panel-players`, đè lên khung chat trên mobile (~2 dòng còn hiển thị) — chuyển sang tab-content riêng `#tab-score`, ẩn/hiện theo cùng khuôn mẫu tab-users; sửa phạm vi 2026-08-14 (bản đầu trỏ nhầm `tournament-match.html`, người dùng xác nhận đúng là `room.html`); `tournament-match.html` giữ nguyên, ngoài phạm vi lần này. `?v=` 119→120, `npm test` 1138/1138, xác minh Playwright thật 2 guest mobile 390×844… | [chi tiết](docs/fix-log/2026-08-14-todo-116-room-score-panel-tab.md) |
| 2026-08-14 14:00 | TODO.md #117: lobby render lại toàn bộ danh sách phòng (`renderRoomList` qua `innerHTML`) mỗi khi nhận `lobby:patch`, dù server (`server/socket/state.js`) đã gửi diff `{ upserts, removed }` sẵn — chi phí tăng theo tổng số phòng × tần suất sự kiện thay vì số phòng thực đổi, rõ nhất khi nhiều người/phòng cùng lúc. Sửa `client/js/lobby.js`: `applyLobbyPatch()` áp diff trực tiếp lên DOM hiện có qua `data-room-id` (`updateRoomRowNode()` cập nhật tại chỗ, không replay animation; hàng mới mới tạo node + animation), chỉ fallback full-render khi vượt biên rỗng↔không-rỗng; `renderRoomList()` giữ nguyên cho full-snapshot/lang/uimode. `?v=` 118→119. Không có Jest cho `client/js/`; xác minh bằng 1 spec Playwright mới giữ lại (`e2e/lobby-patch-incremental-render.spec.ts`) — `MutationObserver` thật trên Chromium: seed 4 phòng, đổi đúng 1 phòng, chỉ `data-room-id` của phòng đó bị đụng, `#room-list` không bị thay thế toàn bộ, số hàng không đổi… | [chi tiết](docs/fix-log/2026-08-14-todo-117-lobby-render-incremental.md) |
| 2026-08-14 16:13 | TODO.md #118: người dùng báo bàn cờ mobile (Safari iOS, ảnh chụp thật) thỉnh thoảng méo/lệch — điều tra (CodeGraph + agent) chỉ ra `.board-area-shell` dùng `100vh` (không `dvh`) cộng `window resize` listener không throttle ở `game-ui.js` bắn lại `BoardRenderer.resize()` đồng bộ mỗi sự kiện, có thể "đóng băng" canvas theo `innerHeight` giữa lúc toolbar Safari đang animate. Không có thiết bị Safari iOS để tái hiện; người dùng chốt sửa phòng ngừa ngay. Thêm `height: 100dvh` (fallback `100vh`) vào `room.css`/`room-zen.css`, gate listener resize qua `requestAnimationFrame`. `?v=` 120→121, `fix/mobile-board-resize-dvh` off `main`. **Chưa xác nhận trên thiết bị thật** — chờ người dùng gốc phản hồi sau deploy… | [chi tiết](docs/fix-log/2026-08-14-todo-118-mobile-board-resize-dvh.md) |
| 2026-08-14 20:56 | TODO.md #121: tên phòng mặc định (không đặt tên tuỳ chỉnh) đổi từ `Phòng của <displayName host>` sang `#<roomID>` theo đúng định dạng người dùng chốt — `RoomManager.js:130` tái dùng biến `roomId` đã sinh sẵn ở dòng trên, không sinh mã mới; nhánh tự đặt tên giữ nguyên; rà `lobby.js:260,304` không cần sửa gì thêm phía client. 2 test mới (`RoomManager.test.js`, describe `'RoomManager — default room name'`), `npm test` 1136/1136. `fix/room-default-name-id` off `main`, không đụng `client/css/`/`client/js/` nên không cần bump `?v=`… | [chi tiết](docs/fix-log/2026-08-14-todo-121-room-default-name-id.md) |
| 2026-08-14 21:34 | TODO.md #119: guest bấm "Create account" trong Settings không vào được form đăng ký — `login.js`'s `checkExistingSession()` bounce mọi session tin là đã có (kể cả guest) về `index.html` ngay khi vào `login.html`, do `hasBelievedSession()` không phân biệt guest/session thật. Sửa: đọc thêm `GvnSession.getUser().isGuest`, chỉ bounce khi KHÔNG phải guest; đã kiểm tra 2 nơi khác gọi `hasBelievedSession()` (`socket-client.js`, `session.js`'s `requireAuth()`) — không cần sửa, hướng ngược lại (page-guard, guest vẫn hợp lệ). 3 test mới trong `login-oauth-error-banner.test.js`, `npm test` 1139/1139. `?v=` 121→122 (đụng `client/js/login.js`). `fix/guest-create-account-bounce` off `main`… | [chi tiết](docs/fix-log/2026-08-14-todo-119-guest-create-account-bounce.md) |
| 2026-08-14 22:24 | TODO.md #120: `login.html` thiếu nút đổi ngôn ngữ — `i18n.js`'s auto-init đã có sẵn logic mount `createLangSwitcher()` nhưng nhắm `.card__logo`, class thuộc layout login cũ mà redesign `page-split`/`login-shell` không còn mang theo, nên switcher âm thầm không mount kể từ đó. Sửa: thêm mount point mới `.login-lang-switch-row` trong `login.html` + CSS căn phải trong `login.css`, đổi đúng 1 dòng selector trong `i18n.js`, không viết lại cơ chế switcher. Xác minh bằng Playwright thật (desktop + mobile 390×844) qua static server cục bộ — không chạy `server/index.js`/đụng database thật. 4 test mới trong `login-lang-switch-mount.test.js`, `npm test` 1143/1143. `?v=` 122→123. `fix/login-language-toggle` off `main`… | [chi tiết](docs/fix-log/2026-08-14-todo-120-login-language-toggle.md) |
| 2026-08-15 11:44 | TODO.md #122: xoá thẻ `<script src="js/profanity-classifier-model.js?v=125">` khỏi `client/room.html:202` — script chết (classifier đã tắt theo quyết định sản phẩm từ trước, 0 ảnh hưởng output đã chứng minh bằng 54 chuỗi thử), chặn parser mỗi lần vào phòng; không đụng file JS hay `profanity-filter.js`. Xác minh Playwright trên server dev đang chạy của người dùng (không restart, không đụng DB thật): 0/102 request là `profanity-classifier-model.js`, 0 console error; chat qua UI không tự động hoá được (quick-match chờ người chơi 2) nên `require()` trực tiếp `profanity-filter.js` chạy lại các chuỗi thử tiêu biểu — output khớp cũ. `?v=` 125→126, `npm test` 1147/1147. `fix/remove-dead-profanity-classifier-script` off `dev`… | [chi tiết](docs/fix-log/2026-08-15-todo-122-remove-dead-profanity-classifier-script.md) |
| 2026-08-15 12:06 | TODO.md #124: `get-client-ip.js:48`'s `X-Forwarded-For` fallback lấy phần tử đầu (`split(',')[0]`, dễ giả mạo nhất) thay vì phần tử cuối — đổi thành `.pop().trim()`, đúng 1 dòng, giữ nguyên ưu tiên `CF-Connecting-IP`/điều kiện loopback. 2 case cũ trong `get-client-ip.test.js` dùng XFF nhiều giá trị (trước đó kỳ vọng phần tử đầu) cập nhật sang kỳ vọng phần tử cuối, thêm 1 case mới. `get-client-ip.test.js` + `LobbyHandler.test.js` 29/29 pass. Không đụng `client/`, không cần bump `?v=`. `fix/getclientip-xff-last-element` off `dev`… | [chi tiết](docs/fix-log/2026-08-15-todo-124-getclientip-xff-last-element.md) |
| 2026-08-15 13:00 | TODO.md #123: thêm `<link rel="preload" as="font" type="font/woff2" crossorigin>` cho `Phosphor.woff2`/`Phosphor-Bold.woff2` vào `<head>` của 6 trang thật, trước `<link rel="stylesheet">` phosphor tương ứng, đúng số weight mỗi trang đang nạp (index/login: chỉ regular; room/tournament/tournament-match/history: cả 2) — nhắm triệu chứng người dùng xác nhận trực tiếp (icon vào muộn trên mạng chậm) qua cơ chế `font-display: swap` phát hiện font muộn qua CSSOM. Không đụng `@font-face`/`font-display`, không thêm `modulepreload` (#126 riêng). `?v=` 126→127 toàn bộ `client/*.html`+`client/js/*.js` (trừ 2 mockup đóng băng), verify grep còn đúng 1 giá trị. `npm test` 1148/1148. **Chưa verify DevTools Network/Console thủ công trên mạng chậm thật** — không có Jest cho `<head>` HTML. `fix/preload-phosphor-woff2` off `dev`… | [chi tiết](docs/fix-log/2026-08-15-todo-123-preload-phosphor-woff2.md) |
| 2026-08-19 21:26 | TODO.md #131: `client/js/socket-client.js` không đặt `timeout` nên dùng mặc định 20 000 ms của socket.io-client — theo HAR người dùng gửi (2026-08-19 20:47), lần WebSocket đầu chết vì mất gói SYN ở chặng trình duyệt ↔ Cloudflare edge (`blocked=44616ms`, `connect=7196ms` dạng SYN retransmit), client chờ đủ 20 s rồi lần thử lại chỉ mất 2.9 s là xong ⇒ ~24 s treo ở "Đang kết nối…". Thêm `timeout: 8000` (đúng 1 dòng), giữ nguyên thứ tự transport websocket-first và mọi tham số `reconnection*`. 8 test mới `client/tests/socket-client-connect-options.test.js` (đã kiểm chứng: bỏ bản sửa ra thì 4/8 fail), `npm test` 1193/1193. Xác minh Playwright trên instance **cô lập** (copy repo, DB tạm, cổng 3111 — không đụng DB/server thật đang có người chơi): luồng guest → tạo phòng → `room.html` connect 839 ms, `io._timeout=8000`, transport websocket, 0 console error; **đo được cả đường thất bại** bằng `routeWebSocket` nuốt handshake — 20 120 ms (không sửa) → 8 122 ms (có sửa), tiết kiệm ~12 s. Ghi chú: đây là **giảm thiệt hại**, nguyên nhân gốc (mất gói client↔edge) không sửa được bằng code; #130 từng bị nghi là nguyên nhân nhưng log đã bác bỏ. Cũng sửa một tuyên bố sai của chính mình trong `docs/*/B131-*.md` ("client/js/ không có hạ tầng test") — `client/tests/` đã có sẵn 9 file jsdom test. `?v=` 130→131. `fix/socket-io-connect-timeout` off `dev`… | [chi tiết](docs/fix-log/2026-08-19-todo-131-socket-io-connect-timeout.md) |
| 2026-08-19 22:04 | TODO.md #131 (retune): người dùng dán console log có 2 cặp `Firefox can't establish a connection to … wss://…/socket.io/`. Chẩn đoán: `mtr` từ máy chủ cho thấy hop 1-5 (router nhà, modem, mạng truy nhập) **0% loss** nhưng từ hop 8 trở đi **16.7% loss** liên tục — `curl -w time_connect` tới Google/GitHub/Cloudflare/site đều ra 45 ms hoặc 3.1/5.2/7.6/11.7 s (bậc SYN retransmit), tức mất gói ở **nhà mạng**, không phải Wi-Fi (ping router 0%, −56 dBm), không phải tunnel, không phải code. Hệ quả: `timeout: 8000` của bản sửa trước **sai** — nó hiệu chỉnh trên 1 mẫu HAR duy nhất (2.9 s), trong khi 12 lần bắt tay WS thật trải 1.9–7.948 s (median 5.07 s), tức 8000 nằm đúng trên đỉnh phân bố và sẽ cắt những lần đáng lẽ thành công. Retune → **12000** theo bậc thang SYN retransmit (1s→3s→7s→15s). Cũng siết biên test: biên cũ `>7948` vẫn cho 8000 lọt (thừa 52 ms) nên test không bắt được chính lỗi vừa sửa → nâng `>10000`; kiểm chứng lại 8000 fail 3/8, 20000 fail 3/8. `npm test` 1193/1193. Playwright trên instance cô lập: connect 767 ms, `io._timeout=12000`, 0 console error; đường thất bại 12 115 ms. Hai nhóm log còn lại vô hại: `AudioContext` = chính sách autoplay, `content.js:64` = tiện ích mở rộng trình duyệt. `?v=` 131→132. `fix/socket-io-timeout-retune` off `dev`… | [chi tiết](docs/fix-log/2026-08-19-todo-131-retune-timeout-12s.md) |
| 2026-08-21 17:55 | TODO.md #133: mobile báo đường kẻ bàn cờ nhạt + bàn cờ nhỏ — `board.js` grid-line alpha 0.22→0.4 (nhánh standard/caro); `resize()`'s `viewportBudget` (zen room, mobile) trừ nhầm budget non-zen cứng `14+16+12+8=50px` thay vì overhead zen thật (`canvasWrapBorder`+`turnBarMargin`+`controlsMargin`), oversubtract xác nhận đúng **48px** qua đo Playwright trên viewport thấp (375×520: 263.4px→311.4px). Xác minh trên instance cô lập (copy repo + DB tạm từ `schema.sql` + cổng 3111 + `CORS_ORIGIN` riêng, không đụng server/DB thật). `client/js/` không có test tự động. `?v=` 123→124, `fix/mobile-board-grid-and-size` off `main`… | [chi tiết](docs/fix-log/2026-08-21-todo-133-mobile-grid-line-board-size.md) |
| 2026-08-21 18:12 | TODO.md #133 vòng 2 (người dùng đo trên **điện thoại thật** sau vòng 1: canvas 476 / shell 500 = 24px, 12px mỗi bên; đồng thời **xác nhận màu grid line 0.4 đã đạt**): vòng 1 chỉ chạm trục dọc nên trục **ngang** còn nguyên — 16px từ `room-zen.css` mobile (`padding: 10px 8px` + override `.zen-drawer-collapsed { padding-right: 8px }`, trạng thái mặc định) + 8px từ `maxVw`'s `- 8` trong `board.js`; số `- 8` có lý do thật cho skin **non-zen** (chống overshoot mẹo full-bleed `width: calc(100% + 32px)` của `room.css`) nhưng `room-zen.css` mobile huỷ đúng mẹo đó nên với zen chỉ là mất trắng → tách nhánh, zen dùng `Math.min(shellWidth, innerWidth) - 2` (2px = hairline `.board-canvas-wrap`, đối xứng với `canvasWrapBorder` trục dọc, không thì viền phải bị `overflow-x: hidden` cắt), non-zen giữ nguyên `- 8`. Đo Playwright 3 viewport: 390×844 canvas **366→388**, wrap phủ đúng `0→390` **sát hai mép**, `scrollWidth === innerWidth` (không sinh cuộn ngang), 0 console error; 375×520 không đổi vì ở đó chiều cao mới là ràng buộc. `npm test` 1143/1143, `?v=` 124→125… | [chi tiết](docs/fix-log/2026-08-21-todo-133-vong-2-truc-ngang-tran-vien.md) |
| 2026-08-21 18:23 | TODO.md #133 vòng 3: người dùng "Line still add more weight, need darker grid" sau khi xem alpha 0.4 (vòng 1) trên máy thật — `board.js` grid line alpha 0.4→0.55 (nhánh standard/caro); border cùng chỗ (0.4, chưa từng đụng) nâng theo lên 0.65 để giữ đúng thứ bậc "border đậm hơn grid" mà comment gốc mô tả, tránh 2 đường trùng alpha sau khi chỉ tăng grid. Không đổi kích thước/layout (chỉ đổi màu). `npm test` 1143/1143, `?v=` 125→126… | [chi tiết](docs/fix-log/2026-08-21-todo-133-vong-3-grid-line-dam-hon.md) |
| 2026-08-21 19:23 | TODO.md #133 vòng 4: merge nhánh fix vào `dev` trước đó **giữ nguyên `?v=133`** (nhánh fix chỉ ở 126, thấp hơn 133 nên tưởng không cần bump) — sai theo đúng quy tắc `git-workflow` skill "re-bump to max(dev, main) + 1"; `board.js`/`room-zen.css` **đổi nội dung thật** lúc merge nhưng URL không đổi ⇒ đúng bug cache-bust mà quy tắc `?v=N` tồn tại để ngăn, client nào cache `js/board.js?v=133` trước merge sẽ giữ mãi bản cũ. Người dùng tự hỏi "v?= should be 134?" sau khi nghe giải thích lý do live site vẫn cũ (server thật chưa pull/restart, xác nhận bằng so khớp byte-for-byte với commit trước merge). Bump `?v=133→134` toàn bộ. `npm test` 1193/1193… | [chi tiết](docs/fix-log/2026-08-21-todo-133-vong-4-quen-bump-v-sau-merge.md) |
| 2026-08-21 19:55 | Người dùng gửi ảnh chụp bàn cờ paper style, hỏi kích thước X/O bằng bao nhiêu % cell rồi yêu cầu tăng — `board.js`'s `_drawBlackPiece()` arm 0.22→0.28×cellSize (bounding box 44%→56%), `_drawWhitePiece()` radius 0.24→0.30×cellSize (đường kính 48%→60%); thử "Large" (0.30/0.33) trước, người dùng phản hồi "Big size" quá nên chốt "Moderate". `lineWidth` giữ nguyên (14% cellSize). `client/js/` không có test tự động. `?v=` 134→135. `fix/paper-symbol-size-increase` off `main`… | [chi tiết](docs/fix-log/2026-08-21-paper-symbol-size-increase.md) |
| 2026-08-21 20:44 | TODO.md #134: sidebar-tab (icon rail zen-skin) đôi khi "thụt vào trong" sau khi kết thúc ván — người dùng tự tái hiện kèm DevTools xác nhận `body.zen-drawer-collapsed` kẹt ở viewport 1920×935. Nguyên nhân: `room-socket.js`'s `game:init` check `matchMedia(max-width:768px)` chỉ **một lần**, không có chỗ nào gỡ class khi viewport rộng lại (bẫy một chiều). Sửa: thêm `matchMedia(...).addEventListener('change', ...)` trong `room.js`, **chỉ gỡ** class khi viewport hết hẹp, không bao giờ tự thêm. 4 test mới `room-zen-drawer-collapsed-recovery.test.js` (xác nhận không rỗng: bỏ fix ra thì 1/4 fail), `npm test` 1147/1147. `?v=` 135→136. `fix/sidebar-drawer-collapsed-stuck` off `main`… | [chi tiết](docs/fix-log/2026-08-21-todo-134-sidebar-drawer-stuck-collapsed.md) |
| 2026-08-21 21:37 | TODO.md #135: người dùng báo icon sảnh chờ "zoom" bất thường — điều tra tìm ra `#129` (migrate Phosphor font → SVG sprite) audit markup nhưng bỏ sót CSS selector nhắm thẳng thẻ `i` cũ, 6 vị trí (`lobby-zen.css`/`room.css`/`room-zen.css`/`history.css`) mất rule `font-size` riêng, rơi về kế thừa từ cha. Sửa cả 6 (`i`→`.icon`, không đổi giá trị khác); bug chỉ tồn tại trên `dev` (`main` chưa merge #129). **Đo Playwright xác nhận bug thật (13px→15px) nhưng KHÔNG khớp độ lớn "zoom" trong ảnh chụp gốc** — đo lại trên production thật trước khi deploy đã cho 15px đúng sẵn, nghi cache trình duyệt/CDN thời điểm đang bump `?v=` liên tục, chưa được người dùng xác nhận lại sau hard-refresh. `npm test` 1197/1197 (không đổi). `?v=` 138→139. `fix/svg-icon-migration-orphaned-selectors` off `dev`… | [chi tiết](docs/fix-log/2026-08-21-todo-135-svg-icon-orphaned-selectors.md) |
| 2026-08-21 22:46 | TODO.md #139 (blocker mobile): nút "Bắt đầu" của start-modal bị bottom sheet che — `.start-modal` z-index 50 (§B36, cố ý thấp) vs `.panel-right-shell` z-index 700 ở nhánh ≤768px; đo Pixel 5 393×727: thẻ bị che 183/210px, `elementFromPoint` tại tâm nút trả về node trong drawer, chạm thật timeout — mà `#start-modal-btn` là lối DUY NHẤT vào trận. Sửa thuần CSS: z-index 750 + neo lớp phủ vào dải trống giữa topnav và sheet (`position: fixed` + `height: max(180px, calc(100dvh - topnav - sheet-h))`, biến thể `--zen-bar-h` khi sheet thu). Verify bằng chạm thật ở 6 viewport (Pixel 5 che 183px→0px), 7 test mới, `npm test` 1150/1150, `?v=135→136`… | [chi tiết](docs/fix-log/2026-08-21-todo-139-mobile-start-modal-behind-sheet.md) |
| 2026-08-21 22:49 | TODO.md #139 merge vào `dev`: nhánh fix off `main` ở `?v=136`, `dev` đang `?v=139` → re-bump toàn repo lên **140** theo `max(dev,main)+1` (không giữ 139: nội dung file đã đổi thật). Xung đột `room-zen.css` giải theo `dev` (`.quick-chat-bar` 650 thay `.btn-focus` 600 đã bị gỡ) rồi ghép rule #139 vào cuối; test `room-zen-start-modal-above-sheet.test.js` đổi rung thang z-index sang `.quick-chat-bar` và đọc số thật từ stylesheet. Đo lại bằng chạm thật trên bản merge: Pixel 5 / iPhone 12 / tablet đều PASS. `npm test` 1150/1150… | [chi tiết](docs/fix-log/2026-08-21-todo-139-mobile-start-modal-behind-sheet.md) |
| 2026-08-21 22:53 | **Đính chính** dòng fix-log #139 phía trên: câu "1150 = 1143 (baseline main) + 7 ⇒ main không có 4 test của #134" là **SAI**. `origin/main` có đủ bản vá #134 lẫn `room-zen-drawer-collapsed-recovery.test.js`; con số 1143 là của `main` **cục bộ đã cũ 4 commit** vì tôi không `git fetch` trước khi đọc — cùng gốc với TODO.md #140 (cũng là báo động giả, đã đóng). Bản sửa #139 và kết quả đo của nó không bị ảnh hưởng: nhánh fix vẫn có ích, chỉ là base cũ hơn main thật (`?v=135` vs 138). Đã fast-forward `main` cục bộ và checkpoint-merge `origin/main`→`dev` (không bump `?v=`, xem #140) | [chi tiết](docs/todo/B140-main-thieu-ban-va-134.md) |
| 2026-08-21 23:47 | TODO.md #136: săn tiếp sau khi báo cáo gốc không tái hiện được — tìm ra **đường thứ hai** và tái hiện được nó: một `?v=` cũ trên cross-import làm trình duyệt nạp **module instance thứ hai** của `room.js`, hai bản listener biến một cú **đổi tab bình thường** thành collapse ở mọi viewport (bản 1 gỡ class + set active, bản 2 thấy `alreadyActive=true` nên toggle đóng). Sửa tầng gốc: tách `activateTab()`/`window.RoomTabs.activate` không bao giờ chạm `zen-drawer-collapsed`, handler click đọc ý định **trước** khi mutate, thêm binding guard `body.dataset.roomTabsBound`, và 2 chỗ `chatBtn.click()` tổng hợp trong `room-ui.js` đổi sang gọi ý định trực tiếp. 9 test mới (gồm 2 test nạp module 2 lần), bỏ bản sửa ra → 7/9 fail; verify trình duyệt thật cả 3 cử chỉ + API ở 2 trạng thái drawer. `npm test` 1213/1213, `?v=140→141`… | [chi tiết](docs/fix-log/2026-08-21-todo-136-tab-activation-vs-drawer-toggle.md) |
| 2026-08-22 00:52 | TODO.md #137: `#start-modal` neo vào `.board-area-shell` với `inset:0`, mà trong zen shell chiếm trọn chiều rộng viewport (chỗ drawer chỉ là `padding-right`, `.room` là `display:block` nên không có cột grid) ⇒ `inset:0` phân giải theo padding box và lớp phủ trải kín cả dải drawer: `z-index:50` so trực tiếp với `15` của `.panel-right-shell` (shell `position:relative` + `z-index:auto` ⇒ không tạo stacking context) và thẻ modal căn theo tâm viewport thay vì tâm bàn cờ. Sửa tầng gốc bằng cách cho lớp phủ tôn trọng đúng `padding` của shell (trùng content box = hộp bàn cờ), **giữ nguyên anchor** (không chuyển sang `#board-area` vì `GameUI.initBoard()` ghi đè `innerHTML`) và **giữ nguyên `z-index:50`** (hạ nó chỉ che triệu chứng). Thêm 1 dòng `inset` vào rule collapsed của nhánh mobile ≤768px vì rule desktop collapsed đặc hiệu hơn 1 class và sẽ rò xuống điện thoại — kiểm chứng: bỏ dòng đó ra thì test mobile fail 393 vs 337 (đúng bề rộng rail). Đo Playwright trên instance **cô lập** (copy repo, DB tạm, cổng 3111, không đụng DB/server thật): chồng lấn lớp phủ × drawer **340px→0** (drawer mở) và **56px→0** (collapsed), lệch tâm thẻ vs canvas **170px→0** và **28px→0**; mobile không đổi một pixel (đúng thiết kế #139). 4 test mới `e2e/start-modal-board-centering.spec.ts` (bỏ bản sửa ra → 2 test desktop fail), `e2e/start-modal-non-blocking.spec.ts` (§B36) vẫn 2/2 pass, `npm test` **1213/1213**. `?v=` 141→142. `fix/start-modal-overlay-respects-drawer` off `dev`… | [chi tiết](docs/fix-log/2026-08-22-todo-137-start-modal-overlay-respects-drawer.md) |
| 2026-08-22 01:02 | TODO.md #138: thu gọn drawer zen chỉ là thao tác **thuần thị giác** (`overflow:hidden` cắt `.panel-right` vốn giữ nguyên bề rộng; mobile thì `translateY` đẩy sheet xuống dưới màn hình) nên nội dung bị cắt **vẫn nằm trong tab-order và accessibility tree** — đi Tab thật từ `#btn-leave` dừng ở `INPUT#chat-input` rồi `BUTTON#btn-send` khi cả hai vô hình, ở cả desktop lẫn mobile. `inert` là thuộc tính DOM nên chỉ đặt được từ JS; thay vì rải lời gọi ở cả 3 nơi đổi `zen-drawer-collapsed` (đúng loại "nguồn sự thật thứ tư" mà §B138 cảnh báo, là thứ #134/#136 đã trả giá), gom thành **một người ghi duy nhất** `setDrawerCollapsed()` + `syncDrawerInert()` trong `room.js`, cả 3 nơi (breakpoint handler, click tab, `room-socket.js` auto-collapse mobile) đều đi qua đó ⇒ class và `inert` không thể lệch pha. Chỉ `.panel-players` + các `.tab-content`, **không bao giờ** `.sidebar-tabs` (rail là cách duy nhất mở lại); chỉ khi có **cả** `zen-room` lẫn class (ngoài zen skin nội dung vẫn hiện); không gate media query nên mobile xử lý y hệt; focus trong vùng sắp `inert` được trả về nút rail của đúng tab đang mở. Đo Playwright trên instance **cô lập**: điểm dừng Tab vô hình **2 → 0** ở cả 1440×900 lẫn 393×727, rail vẫn Tab tới được, quick-chat-bar mobile vẫn trong tab-order, mở lại drawer thì gõ/gửi chat thật OK 0 console error. §B36 kiểm trực tiếp trên trình duyệt (không qua spec `start-modal-non-blocking` vì spec đó flaky sẵn — fail cả trên `HEAD`, đã ghi thành #141). 14 test jsdom mới `client/tests/room-drawer-inert.test.js` (bỏ bản sửa ra → 14/14 fail), `npm test` **1227/1227**. `?v=` 142→143. `fix/drawer-inert-when-collapsed` off `dev`… | [chi tiết](docs/fix-log/2026-08-22-todo-138-drawer-inert-when-collapsed.md) |
| 2026-08-22 10:12 | TODO.md #142 (**người dùng khoanh đúng nguyên nhân**): `.panel-right` là grid **rộng cố định** 339px với `grid-template-columns: 1fr var(--zen-rail-w)`; `1fr` == `minmax(auto, 1fr)` và cái `auto` đó là **min-content của `.panel-players`** — track từ chối co xuống dưới nó. `.slot-card__name` có `white-space: nowrap` ⇒ min-content = trọn bề rộng tên; `min-width: 0` (đã có sẵn) **không** hạ được min-content nội tại mà grid dùng để size track. ⇒ 2 ghế có người tên dài thì cột nội dung phình vượt 339px, **đẩy track rail 56px ra ngoài** và bị `overflow:hidden` cắt — một nguyên nhân sinh **cả hai** triệu chứng báo nhiều vòng: rail "dịch phải/mất padding" và "đường đôi khi collapse" (viền rail lệch khỏi viền shell). Đo 1920×995: tên ~13 ký tự → track `326px`, tràn **43px**, rail còn **12.8px** (khớp DevTools người dùng `21 × 935`); tên 23 ký tự → track `474px`, tràn **191px**, rail **mất hẳn**. **4 vòng điều tra trước trượt vì fixture dùng tài khoản khách tên ngắn** (min-content 277 < track 283 ⇒ không tràn) — tôi đi soi tầng viền/dpr/engine trong khi giá trị sai sinh ở tầng grid track. Sửa 1 dòng: `minmax(0, 1fr)` (đúng thứ đã làm cho `grid-template-rows` ngay dòng dưới), giữ nguyên `overflow`/`flex-end`/width cố định của §B138; mobile ≤768px không dính. 2 test mới `e2e/drawer-rail-not-displaced.spec.ts` dùng **tài khoản thật tên 23 ký tự** (bỏ bản sửa ra → 2/2 fail, Received 58), `npm test` **1227/1227**, #137 spec vẫn 4/4. Hệ quả UX còn lại ghi thành **#143** (nút `✕` bóp tên ở ghế của mình), không gộp. `?v=` 143→144. `fix/drawer-rail-pushed-by-grid-track` off `dev`… | [chi tiết](docs/fix-log/2026-08-22-todo-142-grid-track-day-rail-ra-khoi-drawer.md) |
| 2026-08-22 10:40 | TODO.md #141, 2 phần. **Phần 1 (đua `?id=`)**: `room-socket.js`'s `room:joined` điều hướng sang `/room.html` trước, `?id=` gắn sau qua `history.replaceState` ở nhịp riêng — `waitForURL(/room\.html/)` khớp ngay khi điều hướng xong, chưa đợi `?id=`. Đổi `waitForURL(/room\.html\?id=/)` cho 13 file đọc `?id=` ngay sau không có chờ trung gian; bỏ qua 4 file đã tự an toàn (chờ `#room-id-nav` không rỗng — xác nhận `replaceState` chạy **trước** `RoomUI.updateUI()` set text nav, cùng lượt xử lý) và `undo.spec.ts` (đã có `waitForFunction` riêng). **Phần 2 (2 giới hạn per-IP nhiễu e2e)**: hỏi người dùng qua `AskUserQuestion` trước khi đụng (theo `instruction.md` §B141) — chọn env override chỉ cho harness; thêm `AUTH_LIMITER_MAX` (`server/config.js`, cùng khuôn `MAX_ROOMS_PER_IP`, mặc định 20 khi không set) nối vào `authLimiter` (`server/routes/auth.js`), `windowMs`/`MAX_ROOMS_PER_IP` không đụng. Verify: server throwaway 3111, `start-modal-non-blocking.spec.ts` chromium+firefox **4/4 pass** (từng timeout 100% cả 3 trình duyệt, xác nhận bằng stash-lại-bản-gốc), 12/13 spec khác pass riêng lẻ — 2 fail (`security-boundary.spec.ts` `AUTH_REQUIRED`, `lobby-patch-incremental-render.spec.ts` assertion `touchedRoomIds`) xác nhận không liên quan `?id=`, ngoài phạm vi. webkit vẫn fail như cũ (click không điều hướng) — pre-existing. `AUTH_LIMITER_MAX=5` → request thứ 6 trả đúng 429. 3 test mới `auth-limiter-config.test.js`, `npm test` **1230/1230**. Không đụng `server/db/gomoku.db` thật (db tạm theo `playwright-e2e-safety`, xác nhận mtime gốc không đổi sau khôi phục). `client/`/`?v=` không đổi. `fix/e2e-room-url-race-and-auth-limiter-override` off `dev`… | [chi tiết](docs/fix-log/2026-08-22-todo-141-e2e-room-url-race-and-auth-limiter.md) |
| 2026-08-22 11:01 | TODO.md #143 (hệ quả UX #142 làm lộ ra): nút `✕` (`.slot-card__stand`, `min-width:32px`+`gap:6px`) nằm cùng dòng tên trong `.slot-card__header` (`justify-content:space-between`) bóp tên ghế của chính mình từ ~117px thẻ xuống còn ~70px cho tên trong khi ghế đối thủ (không nút) được trọn dòng — đo được **40px vs 78px** `clientWidth`. `docs/todo/B143-*.md` tự flag "cần bàn trước khi làm" với 3 hướng chưa chốt; hỏi qua `AskUserQuestion`, người dùng chọn đưa `✕` ra khỏi dòng tên. Sửa: `.slot-card` thêm `position: relative`, `.slot-card__stand` đổi sang `position: absolute; top:0; right:0` (góc trên thẻ, chỗ dòng nhãn `#1`/`#2` gần trống), `.slot-card__header` thêm `padding-top:18px` áp **đều cho cả hai ghế** để tên không lệch chiều cao. Giữ nguyên ngưỡng chạm `32px` và `minmax(0,1fr)` của #142 (không đụng, theo đúng ràng buộc `instruction.md` §B143). Đo Playwright 2 tài khoản thật tên 23 ký tự, 2 viewport (1920/1280): sau sửa cả hai ghế **78px**, không chồng lấp. Test mới `e2e/slot-card-stand-name-symmetry.spec.ts` (bỏ bản sửa ra → fail đúng 40px vs 78px); `e2e/drawer-rail-not-displaced.spec.ts` (#142) vẫn 2/2 pass. `npm test` **1230/1230**, `?v=144→145`. `fix/slot-card-stand-squeezes-name` off `dev`… | [chi tiết](docs/fix-log/2026-08-22-todo-143-nut-dung-day-bop-ten.md) |
| 2026-08-22 11:50 | TODO.md #143 vòng 2: người dùng xem trực tiếp bản vòng 1 (`✕` góc thẻ), phản hồi "kiểu cũ inline đẹp/thu hút mắt hơn, kiểu mới làm slot rời rạc" → muốn quay lại inline. Việc đó tái phát đúng bug gốc (tên bị bóp ~40px); hỏi qua `AskUserQuestion` trước khi đụng code — chọn giữ inline nhưng **thu nhỏ nút** thay vì revert nguyên trạng hay giữ full-parity-rời-rạc. Đảo `.slot-card__header` về `justify-content: space-between` nguyên bản, `.slot-card__stand` bỏ `position: absolute`, hạ `min-width/min-height` **32px→24px** — đúng sàn WCAG 2.2 AA "target size (minimum)", không phải số tuỳ ý; `padding` giảm tương ứng. Cải thiện `clientWidth` tên ghế có nút 40px→48px (không full parity với 78px — đúng tradeoff đã chọn), nút đo đúng 24×24px không chồng lấp tên. **Kiểm chứng chạm thật** (bắt buộc theo `instruction.md` §B143 khi hạ ngưỡng chạm): `devices['Pixel 5']` (`hasTouch`/`isMobile`), `page.tap()` thật vào nút 24px → đứng dậy thành công. Viết lại test `slot-card-stand-name-symmetry.spec.ts`→`slot-card-stand-inline-touch-target.spec.ts` (assertion "full parity" vòng 1 luôn sai với thiết kế mới); gặp 1 false-positive do bug trong chính test (`stamp` chứa dấu `-` sinh username không hợp lệ, validate client chặn âm thầm) — sửa xong test ổn định. `e2e/drawer-rail-not-displaced.spec.ts` (#142) vẫn 2/2 pass. `npm test` **1230/1230**, `?v=145→146`. `fix/slot-card-stand-inline-smaller` off `dev`… | [chi tiết](docs/fix-log/2026-08-22-todo-143-vong-2-inline-nho-hon.md) |
| 2026-08-22 19:05 | TODO.md #144: `.topnav` 60px cố định trên zen mobile (9% chiều cao iPhone SE). Hỏi hướng tương tác qua `AskUserQuestion` trước khi code (chọn nút `▾`, không vuốt — tránh pull-to-refresh không kiểm chứng được). **Đo bắt buộc trước khi kết luận** (server throwaway, không đụng DB thật): canvas board **không đổi 1px** ở CẢ Pixel 5 lẫn iPhone SE 375×667 khi bật/tắt nav — sai giả định gốc "iPhone SE lợi thật vì giới hạn chiều cao"; board rộng-giới hạn trên mọi viewport điện thoại thực tế đã đo (chỉ ~2.6px trên iPhone SE 320×568 đời cũ). Báo số đo qua `AskUserQuestion`, người dùng chọn giữ làm tính năng gọn giao diện thuần tuý. Cho xem bản nút `▾`, phản hồi **bỏ nút ▾**, muốn thanh **luôn tối giản**: rời phòng (trái) + mã phòng (giữa) + cài đặt (phải). **Phát hiện giữa chừng**: một icon cài đặt thứ 2 hoá ra được `settings-panel.js` chèn **động** vào `.topnav__right` (không tĩnh trong HTML nên bị bỏ sót lúc đọc code) — xác nhận **không trùng** `tab-settings` như tài liệu #144 nhận định ban đầu (2 phạm vi: cài đặt chung vs cài đặt ván đấu), đính chính tại chỗ trong `docs/todo/`+`docs/instruction/`. Vòng 2: gỡ toggle, `--zen-topnav-h: 28px` cố định, `.topnav__brand` ẩn vĩnh viễn mobile, `.topnav__right` dùng `order` tường minh dàn 3 nút trái/giữa/phải bất kể thứ tự chèn DOM thật. Test `e2e/topnav-minimal-mobile.spec.ts` (2 viewport, bỏ bản sửa ra → fail đúng `received 60`; kiểm tra rời phòng **hoạt động thật**, không chỉ visible). `npm test` **1230/1230**, `?v=147→148→149`. Tự phát hiện lỗi quy trình: bắt đầu sửa trực tiếp trên `dev`, sửa giữa chừng bằng tạo `fix/hide-topnav-mobile` trước khi commit đầu. `fix/hide-topnav-mobile` off `dev`… | [chi tiết](docs/fix-log/2026-08-22-todo-144-topnav-toi-gian-mobile.md) |
| 2026-08-22 20:50 | TODO.md #145: entry `wss://.../socket.io/` dài 543 ms trong HAR, nhưng phân rã ra thì phần lớn nhất **không nằm trong entry** — 462 ms trôi qua TRƯỚC khi socket được mở (riêng 220 ms là client-side sau khi HTML đã về), vì `index-entry.js` là `type="module"` ở cuối `<body>` nên defer, và `io()` chỉ chạy ở cuối đồ thị module dù `_connect()` chỉ cần global `io` + 1 lần đọc `localStorage`. 24/26 request đã từ cache ⇒ không phải vấn đề tài nguyên tĩnh. Tra cứu chuẩn ngành trước khi chọn hướng: đề xuất WHATWG `preconnect` cho `wss://` **closed as not planned** (whatwg/html#8037) ⇒ gọi `io()` sớm hơn là thứ duy nhất platform còn cho phép; Figma/Slack đều thiết kế để socket rời khỏi critical path. **Đính chính** nhận định ban đầu về RFC 8441: trình duyệt CÓ hỗ trợ, CDN thì không — HAR chính là bằng chứng (Firefox xin HTTP/1.1 Upgrade tới edge IP khác trong khi trang chính chạy HTTP/3). Sửa: `socket-early.js` mới chạy trong `<head>` gọi `SocketClient.shared()` (static mới, **một chỗ duy nhất** giữ idempotent; `destroy()` nhả slot); 4 script chuyển lên đầu `<head>` **TRÊN mọi `<link rel="stylesheet">`** — chi tiết quyết định, vì script cổ điển đặt sau stylesheet sẽ chờ stylesheet đó tải xong; gỡ `session.js`/`socket-client.js` khỏi import của `index-entry.js` + 2 hint modulepreload (để lại là nạp file 2 lần = đường #51); `lobby.js` `new` → `shared()`. Object option `io({...})` **không đụng** ⇒ 8 test của #131 pass nguyên. Đo trên 2 instance cô lập (3111/3112, DB rỗng riêng, server thật cổng 3000 và DB thật không bị đụng), mốc `navigationStart` → WS opened, median 7 lần: **36.9 ms → 13.8 ms**; FCP **44 → 24 ms** (rủi ro "4 script đồng bộ vào `<head>` chặn paint" tôi nêu trước khi đo đã bị số đo bác bỏ); đúng **1 kết nối WS/trang**, không bị đá về login, 0 console error, luồng khách→tạo phòng→room→reload→rời phòng chạy giống hệt trên cả 2 instance. **Localhost nên chỉ chứng minh thứ tự đã đổi, không chứng minh biên độ trên domain thật.** 15 test mới (mutation trên bản sao ở thư mục tạm: revert 4 file nguồn ⇒ 13/15 fail); cập nhật hằng số import của `index-entry.js` 7→5 trong test #126 (sanity check trên chính con số, 2 test parity hint↔import vẫn nguyên). `npm test` **1245/1245**, `?v=149→150` (184 chỗ, grep ra đúng 1 giá trị). Phạm vi cố ý chỉ `index.html`; room/tournament vẫn dùng entry module và có test riêng cho đường đó. `fix/socket-early-connect` off `dev`… | [chi tiết](docs/fix-log/2026-08-22-todo-145-socket-mo-som-trong-head.md) |
| 2026-08-22 21:12 | TODO.md #146 (bench cho lệnh GHI `touchSession()` tại handshake, theo yêu cầu `docs/instruction/B146-*.md` "đo trước, đừng tin #81" vì #81 chỉ đo ĐỌC gộp với GHI trong 1 sample). Mở rộng `bench-session-lookup.js`: (1) `touchSession()` cô lập — p50 5-6 µs, p99 <20 µs, cùng bậc với lệnh đọc #81 đã đo ⇒ **đóng #146, không sửa** đúng điều kiện instruction đặt ra trước; (2) dưới tranh chấp WAL thật (connection thứ hai giữ `BEGIN IMMEDIATE`) — **20/20 lệnh block 5006 ms rồi ném `SQLITE_BUSY`** (busy_timeout mặc định better-sqlite3 = 5000 ms), không phải µs, nhưng `grep -rn "new Database(" server/` xác nhận `database.js` là connection **duy nhất** trong production ⇒ kịch bản không reachable. Tách phát hiện đó thành **#149 mới** (địa lôi, không sửa, không cấp bách) thay vì gộp vào #146 — 2 quyết định khác nhau, theo rule "call those out separately" của `CLAUDE.md`. Không thêm `busy_timeout` thấp hơn/retry/connection-pool cho #149 — không có gì đang bị đe doạ trong kiến trúc hiện tại, và không viết test cho kịch bản không dựng được. Không có thay đổi runtime code; commit thẳng trên `dev` theo tiền lệ `8cc2c21` (đóng #81/#85 cùng kiểu, cũng sửa bench script). | [chi tiết](docs/fix-log/2026-08-22-todo-146-touchsession-do-dong.md) |
| 2026-08-22 21:27 | TODO.md #147 (`connectionStateRecovery`). Instruction bắt "dừng và hỏi trước khi code" và bắt trả lời 1 câu hỏi thực nghiệm bằng cách đọc `node_modules` thật ⇒ điều tra trước, đưa checkpoint sau. socket.io 4.8.3, 4 phát hiện: (1) **`skipMiddlewares` mặc định `true`** (`index.js:106-111`) + `namespace.js:214-219` gọi thẳng `_doConnect` bỏ qua `io.use()`; `socket.js:96-107` cho thấy socket phục hồi chỉ khôi phục `id`/`pid`/`rooms`/`data`/`missedPackets`, **không** có `socket.user` (thuộc tính riêng của ta) ⇒ bật "trần" = **TypeError mọi kết nối phục hồi** (`user.displayName` trên `undefined`) + session thu hồi sống lại (phá #68); (2) **`auth.reconnect` CÓ sống sót** (`socket.io-client/socket.js:443` `Object.assign({pid,offset}, auth)`) nên `isOwnReconnect` vẫn đúng — tức mục này *làm được an toàn*; (3) **nhưng lợi ích ~0**: app đã re-gửi TOÀN BỘ state mỗi reconnect (`serializeRoom` + `gameState.serialize()` + `timer.getTimers()` + `timerSync`) nên replay là thừa, và `missedPackets` bắn TRƯỚC `room:joined` ⇒ với event không idempotent (`game:ended`, `room:destroyed`) là phát lại modal cũ; (4) lợi ích thật **duy nhất** = chat bị lỡ (`ChatHandler` không lưu lịch sử, `serializeRoom` không trả chat). Đưa 3 phương án qua `AskUserQuestion`; người dùng chốt **đóng #147, không làm** — dùng cơ chế chạm auth middleware + eviction + 3 grace period để vá một vấn đề nằm gọn trong `ChatHandler` là không cân xứng. Tách khoảng trống chat thành **#150** (ghi lại, KHÔNG làm — theo rule "stack, don't perform"), kèm cảnh báo phải hỏi xem có đáng làm không (không ai báo cáo triệu chứng) và ràng buộc quyền riêng tư (`room:joined` phục vụ cả người quay lại lẫn người vào lần đầu). **Tự đính chính**: dòng "mất hẳn event xảy ra trong lúc rớt" mà chính tôi viết lúc tạo #147 là quá rộng — chỉ chat mất, room/game/timer đều được dựng lại đầy đủ. Không thay đổi code nào. | [chi tiết](docs/fix-log/2026-08-22-todo-147-connectionstaterecovery-dieu-tra-dong.md) |
| 2026-08-22 21:39 | TODO.md #150 (buffer chat gửi kèm `room:joined`). `docs/instruction/B150-*.md` — do chính tôi viết lượt trước — mở đầu bằng "Hỏi trước khi làm: cái này có đáng làm không?" ⇒ điều tra kỹ thuật trước, hỏi sau. 3 phát hiện, 2 trong đó đổi hẳn thiết kế: (1) **`chat:message` là kênh DÙNG CHUNG** — ~30 chỗ emit thông báo hệ thống (`GameHandler` 18, `RoomHandler` 4, `DisconnectHandler` 4, `state.js` 2, `LobbyHandler` 1, `TournamentMatchHandler` 7), phân biệt bằng `isSystem: true`+`code`+`vars`; buffer "tất cả chat:message" sẽ sai vì phát lại "X đã mất kết nối" cho chính X là vô nghĩa; (2) chat phòng giải đấu là **cơ chế riêng** (`tmatch:chat_message`, tự chứa ở `tournament-match.js:791`) ⇒ phạm vi hẹp hơn đã lo; (3) `chat-ui.js:77-81` bắn float toast cho **mọi** tin không phải hệ thống ⇒ nạp buffer N tin qua `appendChatMessage()` = N toast cùng lúc. Hỏi phạm vi qua `AskUserQuestion` (chỉ-người-quay-lại vs mọi-người-vào-phòng, kèm cảnh báo khán giả cũng đọc được ở phương án 2); người dùng trả lời **"có vẻ không đáng làm"** ⇒ **đóng #150, KHÔNG LÀM**. Đúng kết quả mà instruction của chính mục này dự liệu — không ai báo cáo triệu chứng, mục do tôi tìm thấy khi điều tra #147. Giữ lại toàn bộ phần điều tra vì 3 dữ kiện trên mô tả kiến trúc chat hiện tại, không phụ thuộc vào việc có làm buffer hay không. Không thay đổi code nào. | [chi tiết](docs/fix-log/2026-08-22-todo-150-chat-buffer-dong-khong-lam.md) |
| 2026-08-22 | TODO.md #148: middleware chống flood tạo **một `setInterval(1s)` cho mỗi socket**; thay bằng cuộn cửa sổ **tính lười** trong `onevent` (`rollWindows(now)`), giữ nguyên mô hình cửa sổ 1 giây rời rạc chứ **không** chuyển token bucket thuần (token bucket không có khái niệm `violationStreak` theo cửa sổ). Cả hai tầng giữ nguyên, ngưỡng không đổi. Khác biệt cố ý duy nhất: socket flood đủ streak rồi im vĩnh viễn bị ngắt ở event kế tiếp thay vì ở biên — lúc im nó không tốn gì và mọi event vượt ngưỡng đã bị nuốt. **Đo được lại phủ nhận lý do hiệu năng**: event-loop delay và CPU của 0/1000/6000 timer 1s không phân biệt được (số CPU không đơn điệu ⇒ dưới ngưỡng nhiễu) vì Node gom timer cùng duration vào một danh sách; chỉ bộ nhớ là thật (286 B/socket, +1.64 MiB ở 6000). Test 8→20 case (bảng quyết định + biên). `npm test` **1256/1256**, không đụng `client/` ⇒ không bump `?v=` | [chi tiết](docs/fix-log/2026-08-22-todo-148-flood-window-lazy-no-timer.md) |
| 2026-08-23 20:54 | TODO.md #149: đóng theo xác nhận của người dùng ("okay, close 149") — kịch bản kích hoạt (`touchSession()` block 5006 ms khi có connection thứ hai giữ khoá ghi WAL, phát hiện lúc đo #146) không reachable trong kiến trúc hiện tại (`database.js` là connection SQLite duy nhất trong production). Không sửa `busy_timeout`/retry/connection-pool, không viết test giả cho kịch bản không dựng được — đúng như `docs/instruction/B149-*.md` đặt ra trước. Giữ mục lại làm địa lôi. Không có thay đổi runtime code, commit thẳng trên `dev` — tracking-doc-only. | [chi tiết](docs/fix-log/2026-08-23-todo-149-dong-tripwire.md) |
| 2026-08-23 20:54 | TODO.md #151: báo cáo mobile — `#quick-chat-input` (`room.js:317-325`, skin `zen-room`) không có gì gỡ focus khi bấm lại bàn cờ, khiến trình duyệt tự cuộn giữ input trong khung nhìn. Khác chiều với B104 (đã đóng — B104 là bàn cờ vô tình *tạo* focus, B151 là chat *giữ* focus không gỡ). Sửa: `pointerdown` trên `#board-area-shell` tự `blur()` `quickChatInput` nếu đang `document.activeElement` — dùng `pointerdown` chứ không phải `click` (bài học B104: `click` mobile tới muộn ~300ms). Rà soát toàn bộ input khác trong `client/js/`, xác nhận không widget nào khác cùng lỗi (`#chat-input` bị `.panel-right-shell` che kín bàn cờ khi mở). Verify bằng Playwright thật (mobile viewport, guest login + tạo phòng qua UI thật, giả lập tap bàn cờ): focus gỡ đúng. `npm test` 1256/1256 pass. Bump `?v=150→151` toàn repo. Không viết unit test — `client/js/` không có test infra. Code chạm dev-only (`#quick-chat-input` không tồn tại trên `main`) ⇒ `fix/quick-chat-input-focus` off `dev`, merge lại vào `dev`. | [chi tiết](docs/fix-log/2026-08-23-todo-151-quick-chat-blur.md) |
| 2026-08-24 01:30 | TODO.md #152: `game:move` là bare emit không ack — rớt gói `game:move` **hoặc** `game:moved` trong khi socket vẫn "connected" thì client chờ vô hạn, bàn cờ đứng, phải F5 (báo cáo người chơi TQ). Sửa đủ 5 mục: ack 2 chiều (`.timeout(5000)`, **method mới** `SocketClient.emitAck`, không đụng `emit()`; guard `typeof ack === 'function'` cho client cũ còn cache `?v=`), dedupe **`moveId` uuid do client sinh** trong `room._moveAcks` (dọn ở `handleGameEnd`; retry gửi lại **đúng id cũ**), sự kiện `game:resync` tái dùng `buildRoomStatePayload()` tách chung với `room:joined` (không viết lại đường dựng state), retry **đúng 1 lần và chỉ khi TIMEOUT** rồi resync, gap detection `moveCount` phía nhận (chỉ trong nhánh `game:moved`; mọi đường nạp state đầy đủ reset baseline — bẫy resync vô hạn). **Không** bật `retries` toàn cục, **không** siết ping — đúng 2 bẫy file TODO đã cảnh báo. **Phát hiện hạn chế và tách thành #154 thay vì tự nới phạm vi**: gap detection không phá được deadlock 2 người luân phiên vì gói `game:moved` tiếp theo *chính là* nước người kẹt đang chờ; đã xác minh `TimerManager` tick thuần server-side, không có broadcast định kỳ nào làm sự kiện đánh thức. 33 test mới (16 server + 17 client), bỏ fix ra thì **26/33 fail**; `npm test` **1289/1289**. Verify Playwright trên instance **cô lập** (cổng 3199, DB tạm từ `schema.sql`, không đụng DB/server thật) mô phỏng rớt gói bằng cách vá `Socket#packet` — 4 kịch bản qua (`e2e/game-move-ack-resync.spec.ts`, giữ lại). `?v=151→152`. `fix/game-move-ack-timeout-resync` off **`dev`** (mục tracking chỉ có trên `dev`) | [chi tiết](docs/fix-log/2026-08-24-todo-152-game-move-ack-retry-resync.md) |
| 2026-08-24 04:25 | TODO.md #153: `onCellClick` chỉ emit, không vẽ gì — người tự đặt quân cũng phải chờ trọn round-trip mới thấy quân của chính mình (~0.5s đo được, đã loại trừ mọi độ trễ giả trong code). Sửa: `BoardRenderer.optimisticStone` — overlay riêng tách khỏi `this.board` (chỉ server ghi), vẽ bán trong suốt + viền nét đứt **ngay khi click**, trước cả `emitAck()`. Hoà giải qua `game:moved` khớp toạ độ (**không phải ack** — lý do: broadcast luôn tới trước ack cùng kết nối, dùng ack sẽ flash-rỗng nếu chính broadcast bị gap-check #152 chuyển hướng sang resync); ack `error` → rollback miễn phí (chưa từng ghi `gameState.board`); timeout lần 1 → đổi vòng sang "warning", giữ vị trí; timeout lần 2 + `room:joined` (mọi nguyên nhân) → dọn overlay. Thêm chặn "1 nước đang bay" ở `onCellClick` (click thứ 2 trong lúc chờ bị bỏ qua) vì `isMyTurn` phía `board.js` không tự tắt tới khi xác nhận. Đã xác minh **portal không dịch chuyển quân** (`GameEngine.makeMove` ghi thẳng toạ độ click, ô portal còn bị chặn đặt quân) nên optimistic an toàn với `rulePortal`. 29 test mới (13 BoardRenderer + 16 GameUI/room-socket), bỏ fix ra thì **20/29 fail**; `npm test` **1318/1318**. Verify Playwright trên instance cô lập (cổng 3198, DB tạm): quân pending hiện trong **~35-45ms**; rollback thật qua vi phạm vùng nước-đầu-tường (lỗi server thật). **Không ép được đo RTT mô phỏng ~500ms cho phần xác nhận-từ-server** — CDP `Network.emulateNetworkConditions` không throttle được khung WebSocket dù áp trước hay sau khi kết nối (xác nhận cơ chế CDP hoạt động thật bằng cách throttle `fetch()` cùng lệnh, cùng trang: ra đúng ~262ms cho latency=250ms — giới hạn đã biết của Chromium/CDP, không phải lỗi test); nói thẳng theo tiền lệ #126 thay vì báo khống số đo. `?v=152→153`. `fix/game-move-ack-timeout-resync` off `dev` (cùng branch #152, commit riêng) | [chi tiết](docs/fix-log/2026-08-24-todo-153-optimistic-render.md) |
