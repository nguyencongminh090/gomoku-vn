# Fix log entry — 2026-08-04 09:18

## Prompt

TODO.md #29 (unexplained >6000-player connection ceiling, follow-up to today's TODO #41 debounce fix): the item was still open from the 2026-08-02 investigation, which found and fixed one causal contributor (O(n²) `lobby:online_users` broadcast, debounced) but left the ceiling only partially explained — 73-86% success at 6000 concurrent connecting players after that fix, with the real cause still unattributed. Today's TODO #41 (`ONLINE_USERS_DEBOUNCE_MS` 300ms→1500ms) touches the exact same code path, so it was worth re-measuring whether it moved this number.

## Action

Re-ran `scripts/capacity-test/orchestrator.js` at 6000 and 8000 concurrent connecting players (`--rooms=3000`/`--rooms=4000`, `--workers=16`) against the real repo's `server/index.js`, following the CLAUDE.md db-safety procedure (real `gomoku.db` moved aside before any server start, restored + checksum-verified after, throwaway backup files cleaned up). Ran a genuine on/off causal experiment on `--cpu-prof`, not just a single measurement: same source, same 6000-player scenario, with vs. without `node --cpu-prof`. Result: without profiler, both `ONLINE_USERS_DEBOUNCE_MS=300` and `=1500` scored ~100% (0-3 stray errors); with profiler, the identical scenario dropped to 97.6% (71 `connect timeout`). This isolates `--cpu-prof` itself as a real source of the measurement noise in prior sessions — the 2026-08-02 session's 73-86% figures were captured in the same runs used to pull `--cpu-prof` data, so they likely conflate genuine server behavior with profiler overhead. Pushed load further to 8000 concurrent players (unprofiled): reproduced a real, repeatable ceiling — 81.5% then 85.3% across two consecutive runs (740 then 590 `connect timeout`), with an external (non-intrusive) `/proc/PID/stat` CPU sampler (5 samples/sec, doesn't run inside the profiled process) showing peak CPU of ~241% of one core — well under the 800% available on this 8-core machine, consistent with the standing single-thread-bottleneck hypothesis rather than whole-machine CPU saturation.

## Decision

Did not attempt to isolate the ~8000 boundary's exact cause this session — doing so safely requires a profiling method that doesn't perturb the process under measurement (e.g. OS-level `perf record`, or `0x`), neither of which is available in this repo's existing tooling; using `--cpu-prof` again would reintroduce the exact confound just discovered. Following the repo's "tái hiện → đo → mới sửa" rule, made no source code changes — nothing here was confirmed causal enough to fix. Did not check off TODO.md #29 as done: the original ask (attribute the root cause of the connection ceiling) is still open, just at a higher, better-characterized threshold (~8000, not ~6000).

## Summary output

No `npm test` changes (no source/test files touched — investigation only). Two tracked docs updated with dated addenda: `docs/todo/B29-*.md` (today's findings: the `--cpu-prof` confound, the 6000-clean/8000-ceiling split, CPU-sampler numbers) and `docs/instruction/B29-*.md` (methodology lesson: never let one run serve as both the official success-rate measurement and the `--cpu-prof` capture; profiler overhead is large enough at this load to change the pass/fail verdict). Real `server/db/gomoku.db` was moved aside before every server start in this session and restored afterward; final restore verified by md5sum match (`9b6939908f2d3e3d98a218fda7630c29`) and identical `users`/`games` row counts (1/0) to the pre-session state. Three throwaway backup `.db` files auto-generated by an unrelated scheduled `purge-games` process running against the throwaway db during the test window were deleted as cleanup (verified zero real user data — the throwaway db was the only db present at that path during the whole test window); `backups/admin.log`'s matching entries were left untouched as an accurate record of what actually ran, per the log's own append-only convention. No server processes or listeners left running (`ss -ltnp` on :3000/:3001 confirmed clean) after the session.
