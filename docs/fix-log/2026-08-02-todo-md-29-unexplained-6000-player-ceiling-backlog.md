# Fix log entry — 2026-08-02 22:41

## Prompt

TODO.md #29 (unexplained >6000-player ceiling, backlog fix already shipped): re-ran `scripts/capacity-test/` in a fresh session (same 8-core/Node v22 shape as the original stress-test, but a different sandboxed dev container — not directly comparable to the original session's absolute numbers) and reproduced the ceiling — 6000 concurrent connecting players landed 72-78% room success across repeated runs, all `connect timeout`. Isolated the three suspects the item left open by direct measurement, not guesswork: (1) `jwt.verify` — reran the same 6000-player burst against a server with `jwt.verify` monkey-patched to a no-op decode (`--require` preload, never touching tracked source) and got the same 77.5% success (vs. 78.6% unpatched) — **ruled out**. (2) Took a real `--cpu-prof` CPU profile of the server process during the exact failing burst and found `getOnlineUsersList()`/the `lobby:online_users` broadcast (`server/socket/SocketHandler.js`) among the top self-time entries — it rebuilds (`Array.from(sessions.values()).map().sort()`, O(n) over every currently-connected session) and re-broadcasts the *entire* online-users list to the whole lobby room on **every single** connect and disconnect, an O(n) op fired up to n times during a burst of n near-simultaneous connections — i.e. O(n²) total. Confirmed causally, not just correlationally: temporarily no-op'ing both broadcast call sites (scratch copy, not committed) raised 6000-player success from the 72-78% baseline to 83-86% across two runs at the same load. (3) Fine-grained CPU sampling (5 samples/sec via `/proc/PID/stat` deltas, not `ps`'s decaying-average `%CPU` column that the original report's ~26-42% figure came from) showed the server process itself spiking to 100-190% of one-core-equivalent aggregate thread time during the burst — meaningfully higher than previously believed, and left otherwise unexplained after ruling out (1) and fixing (2) (profile also showed ~10% of sample time in `saveGame()`'s synchronous SQLite transaction, but that's concentrated by this harness's own workload shape — every test room ends via resignation on `room:leave` — not confirmed as a general-traffic cost, so left untouched here).

## Action

Applied the confirmed fix for (2): added `broadcastOnlineUsers(io)` to [server/socket/state.js](server/socket/state.js), debounced 300ms via the same per-`io` `WeakMap` timer pattern `broadcastLobbyUpdate()` already uses (`ONLINE_USERS_DEBOUNCE_MS`). [server/socket/SocketHandler.js](server/socket/SocketHandler.js)'s two direct `io.to('lobby').emit('lobby:online_users', getOnlineUsersList())` call sites (on connect and on disconnect) now call `broadcastOnlineUsers(io)` instead, collapsing any burst of connects/disconnects within the window into one rebuild+broadcast. `LobbyHandler.js`'s `lobby:subscribe` handler, which answers one specific socket's request rather than broadcasting to everyone, was left untouched — it's O(1) per request, not the O(n²) burst pattern being fixed.

## Decision

Fixing only the confirmed (2) here, not treating the >6000 ceiling as closed: after the fix, 6000-player success is noisier but higher (73-86% across three fresh runs) — a real, reproducible improvement, but not a full resolution, so TODO.md #29 stays open rather than being struck. (3)'s remaining CPU (beyond both the fixed broadcast and the workload-shape-dependent `saveGame` cost) was not further isolated — that would need a flame-graph-level profiling pass this session didn't have scope for — logged as still-open rather than guessed at, per this repo's "tái hiện → đo → mới sửa" rule.

## Summary output

`npm test`: 299/299 passing (16 suites, +1 new test in `server/tests/SocketHandler.test.js` asserting a burst of 5 connects within the debounce window produces exactly one `lobby:online_users` broadcast; the existing single-connect/single-disconnect test updated to advance fake timers past the new 300ms window). Mutation-checked: reverting `state.js`+`SocketHandler.js` alone (via `git stash`) reproduced the pre-fix immediate-broadcast behavior and failed the new burst test (5 broadcasts instead of 1) as expected; restoring passed again. **Load-verified** with a throwaway server (scratch port, real dev server untouched) and `scripts/capacity-test/`: 4000 players still 100%/0 errors (no regression), 3000 still 100%/0 errors, 6000 improved from a 72-78% baseline (pre-fix, this session) to 73-86% (post-fix, three runs) — real but partial.
