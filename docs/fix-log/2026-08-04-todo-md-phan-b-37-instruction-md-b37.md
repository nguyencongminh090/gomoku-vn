# Fix log entry — 2026-08-04 09:52

## Prompt

TODO.md Phần B #37 / instruction.md §B37 (user manual-test report): the timer never ran during Swap2's opening (`place3`/`p2choice`/`place2`/`p1choice`) — `startGame()`'s `ruleSwap2` branch ([server/socket/handlers/GameHandler.js](server/socket/handlers/GameHandler.js), then lines 576-605) emitted `game:init` with `timer: null` and never called `startTimerForGame()`, which only ran later inside `game:swap2_choice` once colors resolved. Separately, `TimerManager`'s constructor hard-codes `this.activeColor = 'black'` ([server/managers/TimerManager.js:59](server/managers/TimerManager.js#L59)), correct for a normal game (BLACK moves first) but wrong for Swap2, where WHITE always moves first after resolution (`GameEngine._assignColors`) — so once the opening resolved, the clock could tick down the wrong side.

## Action

Implemented exactly the design in `instruction.md` §B37: (1) added `TimerManager.remapForSwap2(realBlackPlayerId, realWhitePlayerId)` — swaps `this.black`/`this.white` only if the placeholder-to-real mapping flipped, updates `blackPlayerId`/`whitePlayerId`, and pins `activeColor = 'white'`. (2) `startTimerForGame(io, room, engine, idOverride)` gained an optional 4th param so the Swap2 branch of `startGame()` can start the clock immediately using `engine.firstPlayerId`/`engine.secondPlayerId` as placeholder black/white ids (colors are still `null` at that point, so the existing `find(p => p.color === ...)` lookup can't be used yet); `game:init`'s payload changed from `timer: null` to the real `timer`/`timerSync`. (3) `game:swap2_place` now calls `timer.switchTurn(...)` + emits `timer:sync` whenever a phase boundary hands the turn to the other placeholder player. (4) `game:swap2_choice`'s `r.done` branch now calls `timer.remapForSwap2(...)` on the existing timer instead of constructing a fresh `TimerManager` (which would have discarded the time already spent during the opening). Client: [client/js/game-ui.js](client/js/game-ui.js) `renderSwap2()` changed from `setTurnBarVisible(false)` to `true` and now labels the two slots by `firstPlayerId`/`secondPlayerId` (color is still `null`); `renderTimers()` gained a branch so turn-highlighting compares `currentTurn` against `swap2.firstPlayerId` while `!colorsAssigned`, instead of the normal `players.find(p => p.color === 'BLACK')` lookup which resolves to `undefined` during the opening. Bumped `?v=39` → `?v=40` across all 4 HTML files + 3 entry-JS import lists.

## Decision

Followed `instruction.md` §B37's placeholder/remap design exactly rather than adding a new `TimerManager` constructor param — `firstPlayerId` is always the mover at construction time, so the constructor's existing `activeColor = 'black'` default is already correct for the first opening move, and no `GameEngine.js`/`DisconnectHandler.js` changes were needed (confirmed both already work unmodified: `handleTimeout`/pause/resume only compare `userId`, never phase). Did not touch the `game:rematch`/ready-window redesign tracked separately as TODO #36 — that work is sequenced to land after this fix per the earlier conflict-check discussion in this session, precisely so its ready-flow rewrite doesn't have to be re-verified against a still-buggy Swap2 timer.

## Summary output

`npm test`: 368/368 passing (was 365). New `describe('TimerManager — remapForSwap2()')` block in [server/tests/TimerManager.test.js](server/tests/TimerManager.test.js), 3 cases: firstPlayer really is BLACK (no swap, `activeColor` → `'white'`), firstPlayer turns out to be WHITE (slots swap, remaining seconds follow the player not the label), and unrelated fields (`mode`/`roomId`) untouched. Mutation-checked: stashed just [server/managers/TimerManager.js](server/managers/TimerManager.js) — all 3 new cases failed (`remapForSwap2 is not a function`); restored, green again. **Verified against the real running app, not just unit tests** — started a throwaway server on port 3099 (`NODE_ENV=test`, separate from whatever fronts the live Cloudflare Tunnel on :3000) and extended [e2e/swap2-opening.spec.ts](e2e/swap2-opening.spec.ts) with timer assertions: turn bar not hidden during `place3`, `timerValues.black` actually decreasing over a 2.2s wait during the opening (proves the clock ticks, not just that it's visible), and after resolution the WHITE player's slot carries `turn-bar__active` (proves `remapForSwap2`'s `activeColor` pin took effect) while BLACK's does not. Mutation-checked the e2e assertions themselves by stashing the three fix files and rerunning against the restarted (buggy) server: failed exactly at the turn-bar-hidden assertion, matching the reported bug verbatim; unstashed, restarted the server, reran — passed on chromium. No games reached a win/draw during this manual check and guest sessions are never persisted (`server/routes/auth.js` comment: "not persisted"), so the shared SQLite file was not touched; the throwaway server process was killed afterward.
