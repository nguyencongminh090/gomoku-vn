# Fix log entry — 2026-08-12 07:53

## Prompt

> Do #107 (trivial) → #106 (the actual fix) → #108a/c → measure again → decide whether #105/#109/#110 are still worth it.

## Action

**Ba bước đầu đã xong từ trước** — kiểm cả index (`TODO.md`) lẫn detail file theo quy tắc
index/detail sync: #107 ✅ (`fix/socket-io-min-js`), #106 ✅ (`fix/static-cache-control`),
#108 ⏳ (a)+(c) xong (`fix/phosphor-bold-font-display`). Không sửa lại gì, chỉ xác nhận.
`grep -rn "?v=" client/*.html client/js/*.js | grep -v mockup` → 112 khớp, **toàn bộ `?v=104`**,
bump đồng nhất.

**Đo lại.** Server thật đang TẮT (domain trả 502, không có tiến trình `node server/index.js`).
Theo quy tắc an toàn DB: md5 `gomoku.db`, dời DB thật + `-wal`/`-shm` sang bên, chạy server
tạm **ở cổng 3001** (cố ý không phải 3000, để tunnel đang chạy không phơi bản tạm ra public),
đo, rồi tắt server, xoá DB tạm, khôi phục DB thật — `md5sum -c` **OK**, còn nguyên 12 users /
64 games.

Đo bằng script crawl đệ quy đồ thị request thật của trang sảnh (fetch `index.html`, bám theo
`import`/`url()`), không chỉ kiểm vài file mẫu.

### Kết quả

Trang sảnh, request trình duyệt thật tải (đã loại `.woff`/`.ttf` fallback mà trình duyệt không
bao giờ tải vì đã có `.woff2`): **25 request / 570 164 B**.

| Nhóm | Bytes | Ghi chú |
|---|---|---|
| text css/js/html — **chưa nén** | 327 486 B | gzip -9 → 79 018 B (**−76%**) |
| `socket.io.min.js` | 46 822 B | socket.io tự gzip → 14 655 B trên dây |
| woff2 | 195 856 B | đã nén sẵn, gzip vô ích |

Header sau #106: `/js/*.js`, `/css/*.css`, `/vendor/**` → `max-age=31536000, immutable`;
`*.html` → `no-cache`; `/api/auth/me` → `no-store` (#66 còn nguyên). **23/25 request đã
`immutable`, chỉ 2 request còn phải hỏi lại origin.**

## Decision

**Phát hiện mới → mở #111.** Request thứ 2 còn revalidate là `/socket.io/socket.io.min.js`:
`Cache-Control: public, max-age=0`, `ETag: "4.8.3"`, conditional GET → 304. Nó **không do
`express.static` phục vụ** (middleware `serveClient` riêng của socket.io) nên nằm ngoài
`staticOptions` mà #106 thêm — đúng dạng "sửa đúng tầng đa số asset đi qua, sót asset đi đường
khác" mà quy tắc root-cause trong CLAUDE.md cảnh báo, và chỉ lộ ra vì lần này đo **cả đồ thị**.
Đã tạo `docs/todo/B111-*.md` + `docs/instruction/B111-*.md`, không tự sửa (quy tắc "stack,
don't perform directly"). Pitfall ghi rõ: URL này **không có `?v=N`** nên không được dán
`immutable`.

**#105 — VẪN ĐÁNG LÀM, giữ ưu tiên.** 327 KB text vẫn rời origin không nén; gzip cắt còn 79 KB
(−76%). CF vẫn brotli cho người dùng cuối nên đây không phải fix cho triệu chứng, nhưng 248 KB
tiết kiệm mỗi lần CF miss là **băng thông upload nhà qua tunnel** — đúng chỗ nghẽn sinh ra các
đỉnh TTFB ~1s đo được ở #106. Rẻ, một middleware, rủi ro thấp.

**#109 — HẠ ƯU TIÊN, ĐỔI PHẠM VI.** `dist/` vẫn kẹt 2026-08-08 trong khi `client/` là 08-12, nay
còn thiếu cả #107 lẫn #108 → bật `NODE_ENV=production` bây giờ sẽ **xoá** các fix vừa làm. Đồng
thời lợi ích chính (gộp request) đã teo vì #106 kéo lần-vào-lại xuống còn 2 request về origin.
Việc cần quyết không phải "bật production" mà là **dựng bước build có kiểm chứng, hoặc xoá hẳn
`dist/`** để bỏ cái bẫy. Cần hỏi người dùng (họ vận hành server thật).

**#110 — ĐỀ NGHỊ ĐÓNG.** Đo thật: full brotli 14 225 B, runtime+`vi` 9 430 B → lợi **4 795 B**,
thấp hơn cả ước tính 8-9 KB trong chính mục đó, ~0,8% của 570 KB. Lại đúng loại asset #106 đã cho
cache vĩnh viễn (tải một lần). Không đáng đổi lấy rủi ro biến `i18n.js` thành bất đồng bộ.

## Summary output

- Không có thay đổi code trong phiên này — chỉ đo và cập nhật tracking docs.
- `TODO.md`: cập nhật #105/#109/#110 với kết luận đo lại, thêm dòng index #111.
- `instruction.md`: thêm dòng B111.
- Mới: `docs/todo/B111-*.md`, `docs/instruction/B111-*.md`.
- DB thật đã khôi phục, `md5sum -c` OK; không còn tiến trình server nào do phiên này tạo.
- **Lưu ý ngoài phạm vi:** server thật đang tắt, `https://play3cr.dpdns.org` trả **502** — cần
  người dùng chạy lại `bash start.sh` (phiên này cố ý không tự khởi động server production).
