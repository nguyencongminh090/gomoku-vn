# Fix log entry — 2026-08-04 08:16

## Prompt

TODO.md #43 / instruction.md §43 (review 12.5, kiểm chứng 2026-08-02): `MAX_ROOMS_PER_IP` đếm quota bằng cách quét `this.rooms` — phòng bỏ hoang (chủ rời/rớt mạng, sole occupant) vẫn giữ chỗ trong `this.rooms` suốt `EMPTY_ROOM_GRACE_MS=20s` để chờ reconnect, nên vẫn tính vào quota của IP tạo ra nó dù không còn ai trong đó, khoá nhầm người dùng chung IP hợp lệ (wifi công ty/trường, NAT nhà mạng) khi đúng lúc có phòng đang trong grace. Người dùng chọn hướng (b) trong 3 hướng reviewer nêu: không tính phòng-đang-grace vào `MAX_ROOMS_PER_IP` cứng, nhưng thêm 1 giới hạn phụ riêng để chặn lách quota.

## Action

Thêm hằng số `MAX_GRACE_ROOMS_PER_IP=3` (mặc định, override qua env) trong [server/config.js](server/config.js). `RoomManager.createRoom()` ([server/managers/RoomManager.js](server/managers/RoomManager.js)) nhận thêm tham số thứ 3 tùy chọn `graceRoomIds` (mặc định `Set` rỗng — giữ nguyên hành vi cũ cho mọi caller khác, kể cả test hiện có không đổi) — vẫn quét `this.rooms` (đúng ràng buộc "không quay lại tally") nhưng tách thành 2 số đếm: `activeCount` (creatorIp khớp, KHÔNG có trong `graceRoomIds`) so với `MAX_ROOMS_PER_IP`, và `graceCount` (creatorIp khớp, CÓ trong `graceRoomIds`) so với `MAX_GRACE_ROOMS_PER_IP` — chặn riêng nếu 1 trong 2 vượt ngưỡng. `LobbyHandler.js`s `room:create` handler build `graceRoomIds` bằng cách duyệt `emptyRoomGraceTimers.values()` (từ `state.js`, map theo userId) lấy đúng field `.roomId` của từng entry, rồi truyền vào `createRoom()`.

## Decision

Không dùng bộ đếm tally riêng (increment lúc tạo/decrement lúc xoá) cho `graceCount` — đúng ràng buộc `instruction.md` §43 chỉ rõ "vẫn phải đếm bằng cách quét", để tránh đúng lớp bug mà `MAX_ROOMS_PER_IP` gốc từng cố tránh (quên decrement ở 1 trong N đường teardown). Không đọc `emptyRoomGraceTimers` trực tiếp trong `RoomManager.js` (sẽ tạo circular require vì `state.js` đã `require` `RoomManager.js`) — thay vào đó nhận `graceRoomIds` như tham số do caller (`LobbyHandler.js`, vốn đã `require` cả 2 module) tính sẵn và truyền vào. Chọn `MAX_GRACE_ROOMS_PER_IP=3` (bằng đúng `MAX_ROOMS_PER_IP`) làm mặc định — cùng bậc độ lớn với quota chính, không tự ý chọn số khác mà không có yêu cầu cụ thể.

## Summary output

`npm test`: 400/400 xanh (+6 so với baseline 394 — 5 case mới trong `RoomManager.test.js` describe "per-IP quota exemption for empty-room-grace rooms" (miễn trừ 1 phòng cụ thể; miễn trừ từng phần với quota trộn active+grace; `MAX_GRACE_ROOMS_PER_IP` vẫn chặn dù active quota còn dư — case chống lách quota theo đúng yêu cầu `instruction.md` §43; `graceRoomIds` của IP khác không miễn trừ nhầm IP hiện tại; không truyền `graceRoomIds` thì hành vi y hệt trước #43), +1 case mới trong `LobbyHandler.test.js` xác nhận đúng `.roomId` được gom từ `emptyRoomGraceTimers` — 4 case cũ trong file này cũng phải sửa lại assertion do đổi signature `createRoom()` từ 2 sang 3 tham số). Mutation-check riêng 2 điểm: (1) gỡ nhánh tách `activeCount`/`graceCount` trong `RoomManager.js` (quay về `fromThisIp` gộp như cũ) → đúng 2/5 case mới đỏ (2 case kiểm miễn trừ thất bại vì phòng-grace lại bị tính vào quota chung); (2) gỡ việc truyền `graceRoomIds` ở `LobbyHandler.js` (quay về gọi `createRoom()` 2 tham số) → 5/6 case liên quan trong `LobbyHandler.test.js` đỏ đúng như kỳ vọng. Khôi phục cả 2 → xanh lại (400/400), `git diff` xác nhận về đúng bản đã sửa. **Đã kiểm bằng server thật + `socket.io-client` thật** (không chỉ unit test) — theo đúng rule Playwright/DB trong `CLAUDE.md`: di chuyển `server/db/gomoku.db` (+ `-wal`/`-shm`) sang `*.pre-verify` trước khi khởi động, server thật chạy port 3097 với `MAX_ROOMS_PER_IP=3`, `MAX_GRACE_ROOMS_PER_IP=2`, `EMPTY_ROOM_GRACE_MS=60000` (kéo dài để có đủ thời gian test) — script tạm (đã xoá sau khi kiểm) qua `POST /api/auth/guest` + `socket.io-client` thật: (1) 3 phòng đầu từ cùng IP tạo OK, phòng thứ 4 bị từ chối đúng thông báo quota cũ; (2) disconnect phòng #1 (sole occupant → vào empty-room-grace) → phòng mới từ cùng IP được tạo OK (đúng hành vi miễn trừ); (3) disconnect thêm phòng #2 (2 phòng cùng trong grace, chạm `MAX_GRACE_ROOMS_PER_IP=2`) → phòng mới tiếp theo bị từ chối đúng THÔNG BÁO MỚI "Bạn có quá nhiều phòng vừa bỏ đang chờ dọn dẹp..." (khác thông báo quota chính), xác nhận đúng nhánh chặn quota-phụ hoạt động, không phải nhánh quota chính. Dừng server thật (kill đúng PID), xoá db tạm rỗng vừa sinh ra, khôi phục lại `gomoku.db` gốc — xác nhận bằng `md5sum` khớp 100% với checksum trước khi di chuyển.
