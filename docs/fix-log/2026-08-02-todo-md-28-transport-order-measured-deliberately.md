# Fix log entry — 2026-08-02 22:45

## Prompt

TODO.md #28 (transport order, "measured, deliberately not applied" pending re-evaluation once the backlog fix — TODO.md #27 — was live): re-measured with `scripts/capacity-test/` in this session rather than trusting the original write-up's numbers, per the item's own instruction. At 4000 concurrent connecting players (the original comparison point), both the default transport order (`['polling','websocket']`) and websocket-first (`['websocket','polling']` + `tryAllTransports`) now hit 100%/0 errors — the backlog fix alone already closed that gap, confirming the original "not yet applied" reasoning held at that load. But pushing further to 6000 players (the load `TODO.md` #29 is investigating) found a real, reproducible gap the original comparison point couldn't show: six interleaved runs (3 default, 3 websocket-first, alternating to control for server-state drift) gave the default order 74-80% success vs. websocket-first's 90-100% — a consistent ~15 percentage point gap, not noise.

## Action

[client/js/socket-client.js](client/js/socket-client.js): `io({...})` now passes `transports: ['websocket', 'polling']` and `tryAllTransports: true` instead of relying on socket.io's default polling-first negotiation. `tryAllTransports` is required on socket.io-client 4.8+ so a client whose first transport attempt fails outright (e.g. a proxy that blocks WebSocket) still falls through to polling instead of failing the connection — the same eventual-connectivity guarantee the polling-first default provided, just trying websocket before polling instead of after. Bumped the shared cache-busting version `?v=35` → `?v=36` across all four HTML files and the three JS entry files (per `CLAUDE.md`).

## Decision

Applying now rather than leaving it as a logged-but-unapplied candidate, because the premise for not applying it has changed: the original decision rested entirely on "the backlog fix already gets the default config to 100% at the load that mattered (4000)" — true, but that comparison point undersold the real gap once #29's investigation pushed load to 6000, where the two orders diverge measurably and default is worse. The proxy-compatibility tradeoff the original write-up worried about is still real and still unmeasured here (this session has no client behind an actual WebSocket-blocking proxy to exercise the fallback path) — but `tryAllTransports` bounds that cost to "one extra failed transport attempt before falling back to polling" for that minority of real clients, not a broken connection, so the downside is bounded while the measured upside is not.

## Summary output

`npm test`: 299/299 passing, unchanged (this is a client-only change; `client/js/` has no unit-test infrastructure, consistent with every other `client/js/` fix in this log — verified instead with a new Playwright spec, [e2e/websocket-first-transport.spec.ts](e2e/websocket-first-transport.spec.ts), which intercepts network requests and the page's `websocket` event to assert a normal connection opens a WebSocket directly (`transport=websocket` in the URL) with zero preceding `transport=polling` handshake requests, and that the connection banner never shows disconnected. Mutation-checked: reverting `socket-client.js` alone (`git stash`) reproduced the old polling-first handshake (5 polling requests before any WebSocket) and failed the new assertion as expected; restoring passed again. **Load-verified** with the same throwaway-server + `scripts/capacity-test/` setup as TODO.md #29: 4000 players stayed 100%/0 errors (no regression at the load that mattered before), 6000 players improved from the default order's 74-80% to 90-100% across three fresh runs.
