# Fix log entry — 2026-08-04 04:56

## Prompt

TODO.md #39 (báo cáo người dùng "Reconnect Logic is not very well"): guest/spectator, hoặc player khi ván chưa `ongoing`, mất kết nối bị đuổi khỏi phòng ngay lập tức (`finalizeNormalLeave` chạy thẳng) trong khi vẫn còn người khác trong phòng — khác hẳn 2 nhánh grace đã có (player trong ván `ongoing`, và sole-occupant). Khi reconnect, `getRoomByUser` trả `null` (đã bị xoá khỏi `userRoomMap`) nên rơi vào nhánh `reconnect` và nhận `room:destroyed` với thông báo sai "Phòng không còn tồn tại" dù phòng vẫn còn người khác.

## Action

Thêm hằng số `SPECTATOR_GRACE_MS = 30_000` (override qua env) trong [server/config.js](server/config.js), map mới `spectatorGraceTimers` trong [server/socket/state.js](server/socket/state.js), và cặp hàm `startSpectatorGrace()`/`cancelSpectatorGrace()` trong [DisconnectHandler.js](server/socket/handlers/DisconnectHandler.js) — cùng khuôn mẫu với `startEmptyRoomGrace`/`cancelEmptyRoomGrace` đã có (không xoá user khỏi `room.users`/`userRoomMap` ngay, chỉ hoãn gọi `roomManager.leaveRoom()` sau `SPECTATOR_GRACE_MS`). `handleDisconnect()` nay có đúng 3 nhánh: player trong ván `ongoing` → `startDisconnectGrace`; sole occupant → `startEmptyRoomGrace`; còn lại (guest/spectator/player khi ván chưa `ongoing`, còn người khác trong phòng) → `startSpectatorGrace`, không còn nhánh nào rơi thẳng `finalizeNormalLeave` ngay lập tức. `SocketHandler.js` gọi `cancelSpectatorGrace(user.userId)` ngay cạnh `cancelEmptyRoomGrace`, trước cả 2 nhánh rejoin, để reconnect trong lúc grace tìm thấy đúng phòng còn nguyên. Không đụng `startDisconnectGrace`/`cancelDisconnectGrace` (đúng theo `instruction.md` §39 — case đó đã đúng).

## Decision

Chọn map/hàm riêng thay vì tái dùng `emptyRoomGraceTimers` cho case này — 2 case (sole-occupant vs. còn người khác) tách biệt về điều kiện bắt đầu, dùng chung map sẽ khiến 1 user vừa là "người cuối cùng" vừa "không phải player trong ván ongoing" trở nên mơ hồ khi suy luận. Chọn `SPECTATOR_GRACE_MS = 30s` riêng (không dùng chung `EMPTY_ROOM_GRACE_MS = 20s`) theo yêu cầu người dùng — dài hơn một chút vì guest/spectator dễ gặp mạng di động chập chờn hơn. Không đổi nhánh `reconnect` trong `SocketHandler.js` (vẫn cần cho case phòng thật sự đã bị huỷ khi client offline — server restart/idle cleanup), đúng ranh giới `instruction.md` §39 chỉ rõ không được xoá.

## Summary output

`server/tests/DisconnectHandler.test.js`: thêm describe block mới "spectator grace period" (6 case: bắt đầu grace thay vì leave ngay, seated player khi game chưa ongoing cũng vào nhánh spectator (không phải nhánh in-game), cancel qua reconnect thì không gọi `leaveRoom`, hết hạn thì leave thật (không huỷ phòng vì còn người khác), disconnect lặp lại không chồng timer, cancel khi không có gì đang chờ trả `false`); sửa lại 1 test cũ ("proceeds with normal leave...") theo đúng hành vi mới (nay start grace thay vì leave ngay); thêm `cancelSpectatorGrace` vào mock `DisconnectHandler` trong `SocketHandler.test.js`. `npm test`: 385/385 xanh (+17 case so với baseline 368). Mutation-check: `git stash` riêng 4 file sửa (`config.js`, `state.js`, `SocketHandler.js`, `DisconnectHandler.js`), chạy lại `DisconnectHandler.test.js` → đúng 7/25 đỏ (toàn bộ describe "spectator grace period" + test cũ đã sửa lại), khôi phục → xanh lại. **Đã kiểm bằng server thật + `socket.io-client` thật** (không chỉ unit test mock) — script tạm (đã xoá sau khi kiểm) dựng server thật ở cổng 3098, `SPECTATOR_GRACE_MS` rút còn 3s cho nhanh: (1) host tạo phòng, spectator join, spectator ngắt kết nối rồi reconnect trong lúc grace (đợi 1.3s) → nhận đúng `room:joined` với đầy đủ `users` gồm cả host lẫn spectator, không nhận `room:destroyed`; (2) spectator ngắt lần 2, đợi qua hết 3s grace + 1s, reconnect → nhận đúng `room:destroyed` (giờ mới đúng vì user đã thật sự bị xoá khỏi phòng), khớp hành vi mong đợi khi grace hết hạn thật.
