# Fix log entry — 2026-08-04 04:23

## Prompt

TODO.md #36 / instruction.md §B36 (user-proposed redesign after reviewing the current start-game flow diagram, 3 rounds of clarification before the item was recorded): the Start-modal ready window auto-opened a 30s countdown the instant both seats filled (`syncReadyWindow`, `server/socket/state.js`), and a separate `#game-overlay` win/loss/draw modal + `game:rematch` event handled game end — the user judged this UX as needing a full redesign, not a small fix.

## Action

Core model change: `bothSeated` no longer opens a countdown by itself — only one seated player clicking "Bắt đầu" (`room:ready`) does, via new `handleReadyClick()`/`clearReadyState()` in [server/socket/state.js](server/socket/state.js) (replacing `syncReadyWindow`/`restartReadyWindow`), and the window shrank 30s→15s (`READY_WINDOW_MS`). Added the 3-miss counting state machine: `room.readyMissCount` (new field, [server/managers/RoomManager.js](server/managers/RoomManager.js)) plus `registerReadyMiss()` (replacing `forceUnreadyPlayersToStand`) — misses 1-2 reset both seats to not-ready with no kick, miss 3 kicks only the seat that didn't click that round (the clicker keeps their seat); `resetReadyPair()` resets the pair (ready flags + miss count) on any seat-occupancy change (sit/stand/kick/leave), including mid-countdown, per the user's explicit example. Removed `#game-overlay`, `showGameOverlay()`, `_getReasonText()`, the `btn-rematch`/`btn-close-overlay` handlers, and the `game:rematch` socket event entirely — `handleGameEnd()` now just resets both seats to not-ready (`ready=false`, `readyMissCount=0`, `readyDeadline=null`), letting the identical room:ready flow re-run from scratch for both a first game and every rematch (deliberately reverses a TODO #35 boundary that used to keep those paths distinct, per the user's explicit sign-off recorded in `instruction.md` §B36). `#start-modal` moved out of the shared `.game-overlay` full-screen-backdrop class into its own `.start-modal` (kept as a sibling of `#board-area`, not a child, since `GameUI.initBoard()` replaces that element's innerHTML wholesale) — centered over the board via `position:relative` on `.board-area-shell` + `position:absolute; inset:0` with `pointer-events:none` on the wrapper and `pointer-events:auto` only on the card, so the seat's stand-up button and chat stay clickable while it shows. `#room-entry-overlay`'s shared `.game-overlay` CSS was left untouched (it's the only remaining consumer). Client (`client/js/room-ui.js` `renderStartModal()`) now computes visibility from `bothSeated` (derived from `roomData.users`) rather than requiring `readyDeadline`, and only renders the countdown once `readyDeadline` is actually set. Added a chat line for the previously-silent `board_full` draw case and reworded the agreement-draw line, since the removed modal was the only place either used to say "draw" at all. Bumped `?v=40` → `?v=41` across all 4 HTML files + 3 entry-JS import lists.

## Decision

Reset `resetReadyPair()` to clear BOTH seats' ready flags (not just the seat that just changed) on every occupancy change — not explicitly spelled out in `instruction.md`, but necessary to prevent a stale `ready=true` from a lone earlier click (seat A ready, waiting for seat B to be filled) from silently auto-starting the very next pairing the instant the new occupant of seat B clicks, skipping their real chance to decide; covered by a dedicated e2e regression test. Kept `readyMissCount`/`registerReadyMiss` counting per **current seat pair**, not per user identity, exactly as the user specified. Did not touch `EMPTY_ROOM_GRACE_MS`/`MAX_ROOMS_PER_IP`/`MAX_USERS_PER_ROOM`, `game:moved`/`timer:sync` mid-game, or `#room-entry-overlay`, per `instruction.md` §B36's explicit "không đụng" list.

## Summary output

`npm test`: 379/379 passing (unchanged total from before — replaced 2 removed `game:rematch` cases' worth of `broadcastRoomUpdate` call-site count in the source-sweep guard test, 17→15, with a comment explaining why). New `describe('RoomManager — ready-window miss counting (TODO.md #36)')` block in [server/tests/RoomManager.test.js](server/tests/RoomManager.test.js): 13 cases covering confirmStart/allReady, miss 1/3 and 2/3 (no kick), miss 3/3 (kicks exactly the non-clicking seat, verified with the clicker on either side of the pair, not always the same slot), reset on stand-mid-countdown, reset on a fresh occupant of a vacated seat, reset on kicking a seated player (vs. a spectator, which must NOT reset it), reset on leaving while seated, and 2 defensive edge cases (fewer than 2 seated, unknown roomId). Mutation-checked the miss-3 case: reverting `notReadyUser` selection to always kick slot 2 turns the "kicks whichever seat is not-ready that round" case red. **Verified against the real running app with Playwright**, not just unit tests: ran the full modified/added e2e set individually against a freshly-restarted dev server (draw-offer, resign-flow, room-lifecycle, spectator-join, swap2-opening, kick-blocked-interrupted, concurrent-move-race, move-validation, and the rewritten rematch-overlay-conflict) — all passed; a batch run of several specs back-to-back on one worker hit the pre-existing `MAX_ROOMS_PER_IP=3` quota limit from reusing one loopback IP (documented in `scripts/capacity-test/README.md`), not a regression, confirmed by re-running the same failing specs alone. Added [e2e/start-modal-non-blocking.spec.ts](e2e/start-modal-non-blocking.spec.ts) — the one Playwright test `instruction.md` §B36 called out as mandatory: chat send and the seat's own stand-up button both still work while `#start-modal` has the `visible` class, both passing. Did not get a clean run of `e2e/real-player-gameplay.spec.ts` (a long-running stress/latency spec) in this session due to this sandbox's background-process lifetime limits killing the dev server mid-run on a tool timeout — flagging this explicitly rather than claiming it was verified.
