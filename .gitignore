node_modules/
.venv/
.agents/
# .claude/ is shared via source control (project skills), except personal/local bits:
.claude/settings.local.json
# Most .claude/skills/* entries are symlinks into .agents/ (Antigravity IDE's local, gitignored
# skill store) and would be dangling for anyone who clones without that store — ignore all of them
# except the real gomoku-vn-specific skill directories, which are worth sharing via git.
.claude/skills/*
!.claude/skills/git-workflow/
!.claude/skills/git-workflow/**
!.claude/skills/playwright-e2e-safety/
!.claude/skills/playwright-e2e-safety/**
!.claude/skills/design-workflow/
!.claude/skills/design-workflow/**
CLAUDE.md.bak
.DS_Store
# Submitted diagnostic runs (TODO.md #168) — real players' IPs, geo labels and
# free-text feedback. Source of truth for the #167 sample, 90-day retention,
# never committed.
server/data/diag-results/
.env
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite-shm
*.sqlite-wal
dist/
*.png
# Third-party wordlist with disputed/unclear license (GPL-2.0 vs MIT claims
# on the same data) — used offline only for classifier training, never
# committed or shipped. See tools/profanity-training/README.md.
tools/profanity-training/data/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/e2e/results/

# Playwright
/playwright/.auth/
